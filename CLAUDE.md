# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

ALFRED is a Windows desktop voice assistant built with PySide6 and the Gemini Live API (`google-genai`). It streams microphone audio to Gemini in real time, plays back the spoken response, and can call a small set of tools (screen capture, continuous screen viewing, webcam capture, persistent memory, sending/reading email, remote commands to other ALFRED instances over MQTT, smart home device control, delegating text tasks to other LLM providers, call termination) that the model triggers by voice. All conversation happens in Brazilian Portuguese, and all identifiers, comments, and UI strings in the codebase are in Portuguese — follow that convention when editing.

The finished course project (`JARVIS COMPLETO/` in the repo root) has since been adapted into this one: its nine `actions/` modules, its visual click locator, its animated sphere UI, its session resumption/reconnection, and its OpenAI Realtime provider all live here now (see the sections below). `JARVIS COMPLETO/` itself is kept as the reference copy and is not imported by anything.

This is a learning/course project (`[CURSO]` comments throughout explain Python/Qt/asyncio concepts for the author). Comments are intentionally verbose for teaching purposes; match the existing density if adding to files that already have it, but don't add new `[CURSO]`-style comments yourself.

## Setup and running

No test suite, linter, or build system is configured in this repo.

```powershell
# Activate the venv (already present in ./venv)
.\venv\Scripts\Activate.ps1

# Install dependencies
pip install -r requirements.txt

# Run the app
python main.py
```

Requires a `.env` file (gitignored) in the project root with:
```
GEMINI_API_KEY=<your key>

# Optional — which voice brain to use: "gemini" (default) or "openai". See
# jarvis/nucleo/config.py (PROVEDOR_IA / usar_provedor_openai) and
# jarvis/openai_realtime/. Changing it requires restarting the app; any value
# other than "openai" falls back to Gemini, so a typo can never silently swap
# providers. OPENAI_API_KEY is the SAME key delegacao_ia already uses:
PROVEDOR_IA=<gemini (default) or openai>

# Optional — only needed for the consultar_cotacao_acao / consultar_historico_acao
# tools (consulta_acoes package), a free key from twelvedata.com:
TWELVE_DATA_API_KEY=<your key>

# Optional — only needed by the clicar_elemento_visual tool
# (jarvis/servicos/visao/localizador_clique.py). Read directly with os.getenv
# there, not through jarvis/nucleo/config.py:
GEMINI_VISION_MODEL=<model id, default "gemini-3.1-flash-lite">

# Optional — controls whether the in-call keyword auth gate ("Coisa") is enforced.
# Default true (the security behavior never changes on its own) if unset; only
# `false` disables it. See jarvis/nucleo/config.py — EXIGIR_AUTENTICACAO:
EXIGIR_AUTENTICACAO=<true (default) or false>

# Optional — only needed for the preparar_email/confirmar_envio_email / ler_emails tools:
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_IMAP_HOST=imap.gmail.com
EMAIL_IMAP_PORT=993
EMAIL_REMETENTE=<sender/account address>
EMAIL_SENHA_APP=<app password, not the account password>

# Optional — only needed for the enviar_comando_remoto tool (rede_jarvis package):
MQTT_HOST=<broker hostname, e.g. a HiveMQ Cloud cluster>
MQTT_PORT=8883
MQTT_USERNAME=<broker username, same on every machine>
MQTT_PASSWORD=<broker password, same on every machine>
TOKEN_REDE_JARVIS=<shared secret, same value on every machine>
NOME_MAQUINA=<this machine's name, e.g. "casa" or "loja">
PASTA_TRANSFERENCIAS_PADRAO=<fallback save folder for received files>
GOOGLE_SERVICE_ACCOUNT_JSON=<path to this machine's Drive service-account key file>
PEDIR_PERMISSAO=true

# Optional — only needed for the controlar_dispositivo_casa tool (casa_inteligente package):
TUYA_ACCESS_ID=<Client ID from the Tuya IoT Platform Cloud Project>
TUYA_ACCESS_SECRET=<Client Secret from the same Cloud Project>
TUYA_API_ENDPOINT=<API base URL matching the Cloud Project's actual Data Center — confirm in its Overview tab, don't guess>

# Optional — only needed for the executar_comando_admin tool (admin_terminal package).
# All have working defaults; only set these to override them:
ADMIN_TERMINAL_NOME_TAREFA=<Scheduled Task name, default "JarvisAdminTerminal">
ADMIN_TERMINAL_TIMEOUT_PADRAO=<seconds, default 30>
ADMIN_TERMINAL_TIMEOUT_LONGO=<seconds, default 300 — used when a command is called with execucao_longa=true>
ADMIN_TERMINAL_TIMEOUT_CONFIRMACAO=<seconds, default 40 — how long a non-whitelisted command waits for voice/notification confirmation before failing safe (denied)>

# Optional — only needed for the identificar_planta tool (identificacao_planta package):
PLANTNET_API_KEY=<free API key from my.plantnet.org, Settings > API key>
PLANTNET_PROJETO=<Pl@ntNet flora/project to search, default "all">
PLANTNET_TIMEOUT_SEGUNDOS=<seconds, default 15>

# Optional — only needed for the consultar_segunda_opiniao_visual tool
# (identificacao_visual package). MISTRAL_API_KEY is shared/reused — it may
# already be set from an earlier, unrelated addition to this .env:
MISTRAL_API_KEY=<API key from console.mistral.ai>
IDENTIFICACAO_VISUAL_MODELO_MISTRAL=<Mistral vision model ID, default "mistral-medium-latest">
IDENTIFICACAO_VISUAL_TIMEOUT_SEGUNDOS=<seconds, default 20>

# Optional — only needed for the enviar_dm_discord / enviar_mensagem_discord tools
# (discord_jarvis package). Requires "Server Members Intent" AND "Message Content
# Intent" enabled under Privileged Gateway Intents in the Developer Portal
# (discord.com/developers/applications) — confirmed live, the connection fails
# explicitly without them. The bot also needs to be invited to a server the target
# people/channels are actually in.
DISCORD_BOT_TOKEN=<bot token from the Developer Portal, Bot tab>
DISCORD_TIMEOUT_CONEXAO=<seconds, default 15>
DISCORD_TIMEOUT_OPERACAO=<seconds, default 15>
DISCORD_TIMEOUT_LISTAGEM_MEMBROS=<seconds, default 30>

# Optional — only needed for voice-activated call start (ativacao_voz package).
# No account or API key needed at all — recognition runs 100% locally via Vosk;
# the package downloads its own Portuguese model (~31MB) automatically on first use
# (cached after that, no re-download). Until that first download finishes, voice
# activation just isn't ready yet — the manual "INICIAR CHAMADA" button always
# works regardless:
NOME_ATIVACAO=<activation word/phrase, default "iniciar chamada" — must be a real
  word the Vosk Portuguese model's closed vocabulary actually contains (confirmed
  live via Model.vosk_model_find_word() — the model can NEVER recognize a word
  outside its vocabulary, not even in grammar-constrained mode; foreign-origin
  proper nouns like "jarvis" are a common case that's absent and silently
  unrecognizable no matter how clearly spoken — this is why "jarvis" was replaced
  as the default). Accuracy is also lower than a dedicated wake-word engine since
  this is generic speech recognition, not purpose-built keyword spotting>

# Optional — how long a call can sit with zero real activity (the assistant
# speaking or executing a function — never just microphone noise) before it hangs
# up on its own, warning by voice first. Read directly by jarvis/nucleo/config.py (unlike
# every other TIMEOUT_* constant in jarvis/gemini/cliente_live.py, which are
# hardcoded, not .env-configurable — this one is env-configurable because the
# feature request explicitly asked for it that way):
TIMEOUT_INATIVIDADE_SEGUNDOS=<seconds, default 300>

# Optional — only needed for the criar_arquivo tool (criar_arquivo package):
PASTAS_PERMITIDAS_CRIACAO=<comma-separated absolute paths, default (if unset) is
  this machine's Desktop, Documents, and Downloads folders — NEVER the whole
  disk. A spoken folder name only ever resolves against this list; a name that
  doesn't match is rejected with the list of allowed folders, never guessed>

# Optional — only needed for the abrir_aplicativo tool (abrir_aplicativo
# package). Does NOT relax the whitelist-only behavior (still only apps the
# Windows aliases, the Start Menu, Get-StartApps or this list already know
# about) — just widens the search to also cover portable programs that never
# show up in the Start Menu:
PASTAS_EXTRAS_APPS=<comma-separated absolute paths, optional — a shallow (folder
  itself + one level of subfolders, never a deep recursive scan) search for
  .exe files, combined with the existing Get-StartApps results>
```

`admin_terminal` also requires a one-time **manual** setup step per machine before its
tool works at all — see its section under Architecture below. This is not part of
`pip install`/`.env` and is deliberately never run automatically.

## Repository layout

Everything under `jarvis/` is source; everything under `dados/` is state this
machine generated. Nothing writes a data file next to its own code.

```
main.py                     entry point (`python main.py`)
config.json                 machine-local preferences (interrupcao)
.env                        credentials (gitignored)
docs/INTEGRATION.md         the package-rewiring contract
dados/                      memoria.json, *_conhecidos caches, admin_fila/, logs/
jarvis/
  caminhos.py               RAIZ_PROJETO / CAMINHO_ENV / CAMINHO_CONFIG_JSON /
                            PASTA_DADOS / PASTA_LOGS / garantir_pasta()
  nucleo/                   config.py, preferencias.py, sinalizador.py, prompts/
  gemini/cliente_live.py    GeminiLiveWorker
  ui/                       janela_principal / janela_chat /
                            janela_envio_arquivo / janela_camera
  servicos/visao/           captura_tela, captura_camera, monitor_continuo
  servicos/email/           remetente (SMTP), leitor (IMAP)
  servicos/memoria/         gerenciador
  pacotes/<pacote>/         one isolated package per integration
```

**Never recompute the project root by counting `.parent` hops** — import from
`jarvis/caminhos.py` instead. The single sanctioned exception is
`jarvis/pacotes/admin_terminal/runner_elevado.py`, which the Scheduled Task runs
as a loose script with the project root absent from `sys.path`, so it cannot
import `jarvis` at all; it computes `parents[3]` and says so in a comment, and
its `_PASTA_FILA` must stay pointed at the same folder as
`admin_terminal/config.py`'s `PASTA_FILA`.

This layout replaced a flat root of 20+ sibling folders (`core/`, `config/`,
`gemini/`, `ui/`, `vision/`, `mailer/`, `memory/`, `interfaces_extras/` plus all
14 packages). Renames from that layout: `main_basic.py` → `main.py`,
`gemini/live_client_basic.py` → `jarvis/gemini/cliente_live.py`,
`ui/main_window_basic.py` → `jarvis/ui/janela_principal.py`,
`ui/chat_window.py` → `janela_chat.py`, `ui/envio_arquivo_window.py` →
`janela_envio_arquivo.py`, `ui/camera_window.py` → `janela_camera.py`,
`config/carregador.py` → `jarvis/nucleo/preferencias.py`,
`interfaces_extras/sinalizador.py` → `jarvis/nucleo/sinalizador.py`,
`vision/screen_capture.py` → `servicos/visao/captura_tela.py`,
`vision/camera_capture.py` → `servicos/visao/captura_camera.py`,
`mailer/email_sender.py` → `servicos/email/remetente.py`,
`mailer/email_reader.py` → `servicos/email/leitor.py`,
`memory/memory_manager.py` → `servicos/memoria/gerenciador.py`,
`memory/memory.json` → `dados/memoria.json`. The `_basic` suffix is gone but its
meaning is not: `main.py`, `jarvis/gemini/cliente_live.py` and
`jarvis/ui/janela_principal.py` are still the three course-project files to edit
as little as possible (see the constraint about them further down).

## Prompts (`jarvis/nucleo/prompts/`)

Every hardcoded instruction text sent to any model (Gemini, Groq, Cerebras,
OpenAI, Mistral) anywhere in the project lives here — centralized in one pass,
with every extraction verified byte-for-byte against the original text before
moving it (hashes for the two big ones, AST-based structural comparison for
the rest; see the session transcript for the verification scripts if this
process ever needs repeating).

`prompts/` is a **package**, not a `prompts.py` module, even though it's
imported and used exactly like one (`from jarvis.nucleo import prompts`,
`prompts.ANUNCIO_ESPONTANEO`) — Python doesn't allow a module and a
same-named package side by side in one directory, and the two long prompts
below needed to live in files inside it. Short prompts (a few sentences) are
constants in `prompts/__init__.py`, organized in commented sections by
source file/package. The two genuinely long, multi-section prompts —
`jarvis/gemini/cliente_live.py`'s full `instrucao_sistema` body (~22k chars)
and its `bloco_autenticacao` — are `.md` files instead
(`gemini_live_sistema.md`, `gemini_live_autenticacao.md`), because a
22k-character single-line Python string would be unreviewable in a diff.

**How the two `.md` files are loaded, and why it's safer than the original
Python pattern**: `_carregar()` reads the file, drops blank lines and lines
starting with `##` (section headers — pure human navigation, exactly like
the old `# IDENTIDADE`/`# PERSONALIDADE` Python comments, never part of the
text sent to the model), then joins every remaining line by appending a
single space to **each** line before concatenating — never relying on a
line's own trailing whitespace. This is deliberately more robust than the
original adjacent-string-literal-concatenation pattern documented below: a
missing trailing space in the `.md` source can no longer jam two words
together, because the loader supplies the separating space itself
regardless of what's in the file. `instrucao_sistema_corpo()` returns text
already ending in `"\n\n"` (the separator before the memory context that
gets concatenated after it in `cliente_live.py`) — that's added inside the
function, not baked into the `.md` file, so the file's own whitespace can be
freely normalized without disturbing that meaningful separator.

**The `enviar_tela_para_gemini`/`enviar_camera_para_gemini` duplication was
unified** into one `prompts.ANALISE_IMAGEM_PONTUAL` template taking
`{origem}` (`"tela"` or `"câmera"`) — the two were byte-identical except for
that one word (both use the article "da", so no other grammar adjustment
was needed).

**Package isolation vs. centralization — a real tension, flagged and then
resolved per explicit instruction**: several of the packages this task
touched (`delegacao_ia`, `identificacao_visual`, `memoria_obsidian`,
`cerebro_reserva`, since removed) are documented elsewhere in this file as deliberately
self-contained/"decoupled on purpose... to be copied to another project
as-is." Importing `jarvis.nucleo.prompts` into them cuts against that
principle. The user asked for this centralization explicitly, naming these
exact packages, so it was done as asked rather than re-litigated — but if
one of these packages is ever actually extracted to another project, its
prompt constants would need to come along from `jarvis/nucleo/prompts/`
too, or be re-inlined at that point.

**What was deliberately left out of this centralization**: `FunctionDeclaration`
descriptions (tool schemas) — these are tightly coupled to their parameter
definitions and scattered by design across every package's own
`obter_function_declarations()`; moving them would be a much larger,
higher-risk refactor unrelated to what was asked. Also left alone: the
dynamic status strings passed through `callback_falar` in
`rede_jarvis/permissoes.py` and `admin_terminal/confirmacao.py` (e.g. "fulano
pediu permissão remota...") — these are event descriptions, not instructional
prompts; they get wrapped by `prompts.ANUNCIO_ESPONTANEO` downstream, but
aren't prompts themselves. `identificacao_planta` (Pl@ntNet) sends no text
prompt at all — image-only API, confirmed by reading the client, not
assumed.

## Architecture

Three-layer flow, entry point `main.py`:

1. **UI layer** — `jarvis/ui/janela_principal.py` (`MainWindow`, `QMainWindow`). Builds the PySide6 window (title, status label, call/analyze buttons, activity log) with an inline QSS stylesheet. Owns a single `GeminiLiveWorker` instance (`self.live_worker`), created fresh on each call and discarded when the call ends. All Gemini-thread signals (`status_recebido`, `erro_recebido`, `chamada_encerrada`, `solicitou_encerramento`, `nivel_audio`) are wired here.

2. **Gemini Live worker** — `jarvis/gemini/cliente_live.py` (`GeminiLiveWorker`, a `QThread`). This is the core of the app:
   - Runs its own `asyncio` event loop (`run()` → `asyncio.run(self.executar())`) so the Qt UI thread never blocks.
   - Opens a `client.aio.live.connect(...)` session against `GEMINI_LIVE_MODEL` and runs three concurrent asyncio tasks: `enviar_microfone` (mic → Gemini via `sounddevice.RawInputStream`), `receber_audio` (Gemini → playback queue + tool-call dispatch), `reproduzir_audio` (queue → speakers via `sounddevice.RawOutputStream`).
   - Echo prevention: while `self.alfred_falando` is `True` (assistant is speaking), microphone input is dropped, both at the sounddevice callback level and again when pulling from the queue, plus a queue flush (`limpar_fila_microfone`) whenever new assistant audio starts.
   - `instrucao_sistema` (system prompt) defines ALFRED's identity/personality and, critically, a voice-based authentication gate: the app only responds to commands after the user speaks a secret keyword ("Coisa" — also documented in README.MD). Never remove or weaken this auth logic without being asked.
   - Tool calls from Gemini (`resposta.tool_call`) are dispatched in `processar_chamada_de_funcao` by name. Two different mechanisms coexist, see **docs/INTEGRATION.md** for the full contract:
     - **Native tools** (defined directly in this file, in `function_declarations_nativas`): `analisar_tela` / `analisar_camera` (single-frame vision) / `salvar_print_tela` (captures the cursor's monitor and writes it to disk — see below; distinct from and doesn't alter `analisar_tela`'s memory-only behavior) / `tirar_foto_camera` (captures the webcam and writes it to disk, same pattern, distinct from and doesn't alter `analisar_camera`'s memory-only behavior), `iniciar_visualizacao_continua` / `parar_visualizacao_continua` (continuous screen streaming), `preparar_email` / `confirmar_envio_email` (SMTP, two-step forced confirmation — see below) / `baixar_anexo_email` / `ler_emails` (IMAP, `pasta` param constrained to the `INBOX`/`SPAM` enum), `salvar_memoria` / `listar_memorias` / `esquecer_memoria` (memory), `enviar_captura_email` / `enviar_captura_discord_dm` / `enviar_captura_remoto` (capture-or-reuse-and-send the last screenshot OR photo, whichever is more recent — see below), `encerrar_chamada`. Adding one of these means: add a `types.FunctionDeclaration` to `function_declarations_nativas`, add a branch in `processar_chamada_de_funcao`'s native `elif` chain, and update `instrucao_sistema`.
     - **`preparar_email`/`confirmar_envio_email` enforce confirmation in code, not just in the prompt** — replaced a single `enviar_email` tool that sent immediately when called. `preparar_email` never sends; it stores a pending draft on `self.email_pendente` (`{destinatario, assunto, corpo, caminho_anexo, criado_em}` — only one at a time, a new `preparar_email` call silently replaces any existing draft) and returns text instructing the model to read the draft back to the user and stop, calling nothing else that turn. The actual SMTP send only happens inside `confirmar_envio_email`, and only when `self.email_pendente` is still set (cleared after every outcome — sent, cancelled, or expired) — there is no code path where a tool call can trigger a send without a `preparar_email` → (model reads it back) → `confirmar_envio_email(confirmar=true)` sequence, so a model that acts before finishing listening literally cannot skip straight to sending. `TIMEOUT_RASCUNHO_EMAIL` (120s, same naming convention as `TIMEOUT_VISUALIZACAO_CONTINUA`) discards a draft nobody confirmed in time, checked lazily inside `confirmar_envio_email` (no background timer) — a stale draft just sits inert in memory until then, which is fine since it's a few small strings.
     - **Package tools** (owned by an isolated package like `rede_jarvis`, e.g. `enviar_comando_remoto` / `responder_permissao_remota` / `listar_maquinas_remotas`): the package's `FunctionDeclaration`s live in the package's own `__init__.py` (`obter_function_declarations()`), not in this file. Dispatch tries `pacote.despachar(nome, args)` for every package in `PACOTES_REGISTRADOS` (a module-level list right after the imports) before falling through to the native `elif` chain — `despachar()` returns `None` when a package doesn't recognize the tool name, `str` otherwise, and is always run via `asyncio.to_thread` since it may block. Adding a whole new package (or a new tool to an existing one) means editing the package's own files and, for a brand-new package, adding one import + one line to `PACOTES_REGISTRADOS` — nothing else in this file changes. This is deliberate: `jarvis/gemini/cliente_live.py` is one of the three course-project files that will be entirely replaced once the finished course project lands, so package integration needs to be trivial to re-paste elsewhere — see docs/INTEGRATION.md for the exact copy-paste snippet and any package-specific extra wiring (e.g. `rede_jarvis`'s two session-glue callbacks, documented there, not here).
   - Repeated/near-simultaneous single-frame vision-tool calls are throttled via `executando_funcao_visual` (mutex) and `COOLDOWN_FUNCAO_VISUAL` (per-function debounce). This mutex is separate from continuous viewing, which has its own active/inactive state (`self.monitor_tela_continuo`).
   - Continuous screen viewing streams frames to Gemini via `session.send_realtime_input(video=...)` (not `send_client_content`, unlike the single-frame tools) while the mic keeps recording, so it doesn't interrupt the conversation. The `GeminiLiveWorker` owns at most one active `MonitorTelaContinuo` (`jarvis/servicos/visao/monitor_continuo.py`) at a time in `self.monitor_tela_continuo`; it's torn down both by `parar_visualizacao_continua` and, defensively, in `executar()`'s cleanup block when the call itself ends, so it never keeps streaming frames after the session closes.
   - Blocking calls made from inside the async worker (`capturar_tela_bytes`, `capturar_camera_bytes`, `capturar_monitor_do_cursor_bytes`, `enviar_email` — now called from inside `confirmar_envio_email`'s branch, not `preparar_email`'s) are always wrapped in `asyncio.to_thread(...)` — never awaited directly — since they're synchronous I/O and would otherwise stall the event loop (mic/playback tasks included).
     - **This rule was violated at all 7 call sites of `capturar_camera_bytes()`/`capturar_monitor_do_cursor_bytes()` in this file until a real bug fix** — every one of them called the capture function directly (synchronously, on the event loop), each with a comment falsely claiming this was an intentional, already-validated pattern ("chamada direta, sem asyncio.to_thread, mesmo padrão já usado..."). `jarvis/servicos/visao/captura_camera.py`'s `capturar_camera_bytes()` has an explicit `time.sleep(0.8)` (webcam warm-up) plus opening/reading 10 frames/releasing the device — genuinely slow, not a fast call that could get away with skipping `to_thread`. Every direct call froze the entire event loop (mic capture, audio playback, inactivity checking — everything) for the full duration of the capture. Root-caused live: the user reported "ele responde, executa alguma ação mas depois ele para de me ouvir, demora muito para ele me escutar novamente" — an ongoing, previously-uninstrumented complaint from earlier in the same session (a `DEBUG_TIMING_MICROFONE`/`DEBUG_TIMING_DISPATCH` investigation had been started and paused with zero data collected) — and then, concretely, immediately after a `consultar_segunda_opiniao_visual` (camera) call specifically. That pinpointed the camera-capture path, and code review found the violation at every call site (`_obter_ou_capturar_ultima_captura`, `identificar_planta`/`consultar_segunda_opiniao_visual`'s capture-before-dispatch, `salvar_print_tela`, `tirar_foto_camera`, `enviar_tela_para_gemini`, `enviar_camera_para_gemini`) — all fixed the same way (`await asyncio.to_thread(capturar_camera_bytes)` / `capturar_monitor_do_cursor_bytes`), all now consistent with `jarvis/servicos/visao/monitor_continuo.py`'s `MonitorTelaContinuo`, which already correctly wrapped its own `funcao_captura` call in `asyncio.to_thread` — that class was never affected by this bug, only the one-shot capture call sites in this file were. Confirmed the actual blocking duration by timing `capturar_camera_bytes()` directly, standalone: **2.8 seconds** on this machine's webcam (not just the `time.sleep(0.8)` — device open + 10 frame reads + release + JPEG encode add up) — every single direct call site was freezing the whole event loop for that long, every time. If a similar "responds fine, then goes deaf for a while" report ever resurfaces, check first for a new blocking call made directly on the loop rather than re-opening this same investigation from scratch.

3. **Supporting modules**:
   - `jarvis/nucleo/config.py` — loads `.env` via `python-dotenv`, exposes `GEMINI_API_KEY`, `GEMINI_LIVE_MODEL`, `GEMINI_LIVE_MODEL_FALLBACK`, `GEMINI_VOICE`. Available model/voice options are listed in comments here — swap the active value rather than adding new config plumbing. `GEMINI_LIVE_MODEL_FALLBACK` (`gemini-2.5-flash-native-audio-preview-12-2025`, confirmed live against `client.models.list()` before use, not guessed) is a second, explicit user request: `GeminiLiveWorker._conectar_sessao_gemini` tries `GEMINI_LIVE_MODEL` first and only tries this one if the *connection itself* fails — never on a mid-call failure (see the constraint below). Only if both fail does `self.encerrou_por_falha` get set and the existing full-session cérebro-reserva handoff (see below) take over, exactly as it already did for any other pre-session failure. **Follow-up fix, same feature**: the fallback-model logic alone only helps when a bad connection attempt actually *raises* — it did nothing for a connection that just hangs, never succeeding or failing (the user hit this directly: a call starting while Gemini was already unreachable left the whole app stuck on "Conectando ao Gemini Live..." forever, since no call task exists yet at that point to detect anything wrong). `TIMEOUT_CONEXAO_GEMINI_SEGUNDOS` (15s) now wraps each connection attempt in `asyncio.wait_for`, so a hung attempt gets treated exactly like a raised connection error — tries the fallback model next, and only falls through to the reserve if both time out.
   - `jarvis/servicos/visao/captura_tela.py` / `jarvis/servicos/visao/captura_camera.py` — each exposes one function (`capturar_tela_bytes` / `capturar_camera_bytes`) that captures a single frame (via `mss` / `cv2`) and returns in-memory JPEG bytes (via `Pillow`). No files are ever written to disk. These bytes get sent to Gemini as `inline_data` parts alongside a per-call text instruction that tells the model to only use the freshly sent image.
     - `captura_tela.py` also has `capturar_monitor_do_cursor_bytes()` — same capture/JPEG-encode logic (factored into a small private `_screenshot_para_jpeg_bytes()` helper so it isn't duplicated), but targets whichever physical monitor the mouse cursor is currently on (`win32api.GetCursorPos()` checked against each `mss.monitors[1:]` rectangle), not the fixed primary monitor `capturar_tela_bytes()` always uses. Added for multi-monitor setups, where "the screen" the user means when they say "veja minha tela" is whichever monitor they're actually looking at/pointing at, not necessarily monitor 1. Falls back to the primary monitor if the cursor's coordinates don't land inside any detected rectangle (rare — DPI/resolution-transition edge case), rather than raising. `capturar_tela_bytes()` itself is untouched and still used as-is by anything that doesn't need this (currently: `rede_jarvis`'s remote screen viewing — see below).
     - `captura_tela.py` also has `salvar_print_bytes(imagem_bytes) -> caminho_arquivo` — the one function in this file that *does* write to disk, deliberately separate from every capture function above (all of which stay memory-only, feeding the `analisar_tela`/`analisar_camera`/continuous-viewing/cross-check flows). Backs the `salvar_print_tela` tool specifically, not `analisar_tela` — that native tool's own no-disk-write behavior is untouched (see the "Avoid unnecessary disk writes" constraint below, and don't collapse this new function's disk write into a capture function that other callers expect to stay memory-only). Resolves the *actual current* Desktop path via the Windows known-folders API (`win32comext.shell.shell.SHGetKnownFolderPath(shellcon.FOLDERID_Desktop, ...)`, confirmed live — including after PySide6 has already initialized COM, so no `win11toast`-style WinRT conflict here) rather than assuming `Path.home() / "Desktop"`, since Desktop can be OneDrive-redirected elsewhere. Saves into `<Desktop>/JarvisRecebidos` (created on demand) with a `print_<timestamp>.jpg` name, and a small local `_caminho_sem_sobrescrever()` (same idea as `jarvis/servicos/email/leitor.py`'s attachment one, independently copied) appends a counter on the rare same-second collision, so a save can never silently clobber an earlier one.
     - `captura_camera.py` has the equivalent `salvar_foto_bytes(imagem_bytes) -> caminho_arquivo`, backing the `tirar_foto_camera` tool specifically (not `analisar_camera`, whose no-disk-write behavior is untouched) — same `<Desktop>/JarvisRecebidos` folder, same `_caminho_sem_sobrescrever()`-guarded timestamp naming, just `foto_<timestamp>.jpg` instead of `print_<timestamp>.jpg` so both origins coexist in the same folder without colliding. `_obter_pasta_area_trabalho()` and `_caminho_sem_sobrescrever()` are independently copied here rather than imported from `captura_tela.py`, matching this project's per-module duplication convention (same as `jarvis/servicos/email/leitor.py`'s own copy) — deliberately, not an oversight.
   - `jarvis/servicos/visao/monitor_continuo.py` — `MonitorTelaContinuo`, a standalone asyncio loop (no Gemini/PySide dependency, reusable in other projects) that repeatedly calls an **injectable** capture function (`funcao_captura`, a constructor parameter defaulting to `capturar_tela_bytes` — the original, only behavior before multi-monitor support) at a configurable interval and hands each frame to an injected async callback, until `parar()` is called or a configurable `timeout_segundos` (default 90s) elapses — in which case it self-stops and invokes an optional `callback_encerrado` so the caller can react (e.g. tell the user it timed out). `funcao_captura` is re-invoked fresh on *every* loop iteration by design, not cached from when the loop started — see the two call sites below for why that distinction matters:
     - **Local continuous viewing** (`GeminiLiveWorker.iniciar_visualizacao_continua()` in `jarvis/gemini/cliente_live.py`) passes `funcao_captura=capturar_monitor_do_cursor_bytes`. Since it's re-evaluated every frame, moving the mouse to a different monitor mid-demonstration makes the very next frame follow — this was a deliberate design choice (confirmed with the user before building it, since the same class is shared with the remote-viewing use below, and "does switching monitors mid-explanation confuse the analysis" was a real open question raised before implementing, not assumed away).
     - **Remote screen viewing** (`jarvis/pacotes/rede_jarvis/visualizacao_remota.py`) instantiates the same class **without** passing `funcao_captura`, so it keeps the original default (`capturar_tela_bytes`, fixed primary monitor) — deliberately unchanged, since "wherever the cursor is" doesn't obviously map onto "what a *remote* viewer should see" the same way. Don't change this call site to cursor-follow without an explicit decision from the user; the whole point of making `funcao_captura` a constructor parameter (rather than just swapping the hardcoded call) was to let these two consumers diverge safely from one shared class.
   - `jarvis/servicos/email/remetente.py` — `enviar_email(destinatario, assunto, corpo, caminho_anexo=None)`, a standalone SMTP sender (stdlib `smtplib`/`email` + its own `load_dotenv()` call — deliberately decoupled from `jarvis/nucleo/config.py` so the file can be dropped into another project as-is). Reads `EMAIL_SMTP_HOST`/`EMAIL_SMTP_PORT`/`EMAIL_REMETENTE`/`EMAIL_SENHA_APP` from `.env`. Credentials belong in `.env` (gitignored), not `memoria.json` — that file is for non-secret user memories only. Returns a human-readable Portuguese string in all cases (success, missing config, invalid input, SMTP/connection failure) rather than raising, matching the convention used by `gerenciador.py`, since the return value is spoken back to the user as the tool result.
     - `caminho_anexo` (optional) is validated *before* any SMTP connection is attempted: file must exist, and must be under `LIMITE_ANEXO_MB` (18MB raw — kept under the ~25MB most SMTP providers cap the *encoded* message at, accounting for base64's ~37% size inflation) — a clear Portuguese error is returned instead of letting the SMTP server reject an oversized message. Uses `EmailMessage.add_attachment(...)` (the modern stdlib API, already the class this file uses for the rest of the message) rather than manually wiring up `email.mime.*` classes — same MIME capability, less code.
     - **Verified against a real SMTP send, not just validation logic**: tested end-to-end with a real file actually selected in Windows Explorer (`explorador_windows.obter_arquivo_selecionado()` → `enviar_email(..., caminho_anexo=...)`), confirmed delivered with the attachment.
   - `jarvis/servicos/email/leitor.py` — `ler_emails(quantidade=5, apenas_nao_lidos=False, pasta="INBOX")`, same standalone/`load_dotenv()` convention as `remetente.py`, using stdlib `imaplib`/`email`. Reuses `EMAIL_REMETENTE`/`EMAIL_SENHA_APP` from `.env` (same Gmail app password authenticates both SMTP and IMAP) plus `EMAIL_IMAP_HOST`/`EMAIL_IMAP_PORT`. Opens the mailbox `readonly=True` and fetches with `BODY.PEEK[...]` so listing never marks messages as read. For the `SPAM` folder it doesn't hardcode a folder name (`"[Gmail]/Spam"` varies by account language) — it resolves it dynamically via IMAP `LIST`, looking for the folder with the special-use `\Junk` flag, falling back to the literal name only if that lookup fails.
     - `listar_anexos_disponiveis(quantidade=10)` / `baixar_anexo(criterio_busca, nome_arquivo=None)` — added later, alongside `PASTA_DOWNLOADS_EMAIL` (mirrors `rede_jarvis.config.PASTA_TRANSFERENCIAS_PADRAO`'s pattern: `.env`-overridable, defaults to `Downloads/JarvisEmail`). Connection/login/folder-select logic was factored into a shared `_sessao_imap()` context manager so `ler_emails` and the two new functions never duplicate that setup (a deliberate, behavior-preserving refactor of `ler_emails` done specifically for this — not a rewrite). Detecting attachment names requires downloading the *whole* message (`BODY.PEEK[]`, still never marks-as-read) and walking its MIME parts (`msg.walk()`, `get_content_disposition() == "attachment"`), since IMAP header-only fetches (what `ler_emails` itself still uses) can't reveal attachment filenames.
     - `baixar_anexo`'s `criterio_busca` resolves the same way `casa_inteligente.dispositivos_tuya.resolver_dispositivo` resolves a spoken device name — accent/case-insensitive exact match first, then partial (substring, either direction) against sender-or-subject — copied as a small local `_normalizar`/`_filtrar_emails_por_criterio`, not imported from `casa_inteligente`, since `jarvis/servicos/email/` is deliberately standalone (same reasoning as `gerenciador.py` and `casa_inteligente` each keeping their own `_normalizar` instead of sharing one). `"mais recente"`/`"último"` is handled as a special case before the fuzzy match, picking the single most recent email that has an attachment. Zero matches → clear failure message; more than one match → the full candidate list is returned instead of guessing, for the model to ask the user which one (never auto-picks, per explicit requirement).
     - **Attachment filenames are untrusted input — sanitized at the point of extraction, not just before writing.** A `Content-Disposition` filename is fully attacker-controlled by whoever sent the email; used raw in `PASTA_DOWNLOADS_EMAIL / nome`, a crafted name (`"../../../.env"`, or an absolute path — `pathlib`'s `/` operator discards the left operand entirely when the right side is absolute, making this worse than it looks) would write outside the intended folder. `_nome_arquivo_seguro()` (`os.path.basename` + `re.sub` down to `[A-Za-z0-9._- ]`, applied once in `_listar_nomes_anexos` so listing/matching/saving all agree on the same safe name) plus a resolved-path-must-be-inside-`PASTA_DOWNLOADS_EMAIL` check right before the actual `open(..., "wb")` are both required — this was caught by an automated security review mid-implementation and fixed before considering the feature done, not something to assume is unnecessary if this code is ever touched again.
     - Downloaded attachments are **never** opened, executed, or parsed automatically — `baixar_anexo` only writes bytes to disk and says so in its own return text, and `instrucao_sistema` reinforces treating the content as untrusted. Never overwrites an existing file of the same name — appends a timestamp suffix (and a counter, if that still collides) via `_caminho_sem_sobrescrever`.
     - **Verified against a real inbox, not just unit tests**: `ler_emails` re-tested post-refactor to confirm no regression; `baixar_anexo` tested with a real email and a genuinely *approximate* search term (`"minato"` matching sender "Minato Namikaze", not the full string) — correctly downloaded the real attachment, and correctly appended a timestamp suffix on a repeat download instead of overwriting. The path-sanitization fix above was also verified against real attack-vector strings (`../../../../Windows/System32/evil.dll`, a bare Windows absolute path, `..`, `.`, empty string) — every case resolved to a filename confined inside `PASTA_DOWNLOADS_EMAIL`.
   - `jarvis/servicos/memoria/gerenciador.py` — persistent memory store backed by `dados/memoria.json` (created on demand), guarded by a `threading.Lock` for read/write safety and atomic writes (write to `.tmp`, then `Path.replace`). Public API: `salvar_memoria`, `listar_memorias`, `esquecer_memoria`, `contexto_memorias` (injected into the system prompt at session start). Enforces `MAXIMO_MEMORIAS` (50 entries) and `MAXIMO_CARACTERES` (200 chars/entry), de-duplicates via accent/case-insensitive normalization (`_normalizar_texto`), and refuses bulk-delete phrases like "esquecer tudo" as a safety guard. `esquecer_memoria` resolves a reference by exact ID, then exact text match, then unique partial match (asks for disambiguation if multiple candidates match).
   - `jarvis/pacotes/rede_jarvis/` — a self-contained package for remote command/control between machines running ALFRED, using MQTT as the transport (no VPN/relay server needed — any cloud MQTT broker works, e.g. HiveMQ Cloud). Deliberately isolated from the rest of the app, and integrated with `jarvis/gemini/cliente_live.py` purely through the standard package contract (`obter_function_declarations()`/`despachar()` — see **docs/INTEGRATION.md**) plus its own session-glue callbacks (`_falar_rede_jarvis`, `_receber_frame_remoto`, also documented in docs/INTEGRATION.md) — no other business logic lives outside the package. See `jarvis/pacotes/rede_jarvis/__init__.py`'s docstrings for the full module breakdown (`mqtt_client`, `mqtt_listener`, `comandos`, `visualizacao_remota`, `transferencia_arquivos`, `permissoes`, `notificacoes`, `config`). Notable design points, in case this needs revisiting once the finished course project lands:
     - **Transport history, in case this comes up again**: this package went through two failed Telegram-based designs before landing on MQTT. First, one shared bot for every machine — broken because a bot never receives, via `getUpdates`, messages it sent itself, so nothing ever round-tripped. Second, one bot per machine in a shared group with privacy mode disabled — still broken, because Telegram's Bot API silently never delivers one bot's messages to another bot regardless of privacy/admin settings (undocumented, confirmed by direct testing: a human's messages were delivered fine, a bot's never were). MQTT has no equivalent restriction — any client publishing to a topic is delivered to every other subscriber of that topic, including other bots/services — which is why the package now uses `paho-mqtt` (MQTT5, for its per-message "user properties") instead of a chat platform's API.
     - The listener starts from `GeminiLiveWorker.__init__` (not `main.py`, to keep the "only touch `cliente_live.py`" constraint) — idempotently, via a module-level flag in `mqtt_listener.py` — but `iniciar_rede_jarvis()` *always* re-registers the voice/frame callbacks on every call, since `GeminiLiveWorker` instances (and their live session) are discarded between calls; a stale callback would otherwise keep pointing at a dead session.
     - Unlike the earlier Telegram implementation, `mqtt_client.py`'s `publicar_*` functions are plain synchronous, thread-safe calls (paho-mqtt's `loop_start()` owns its own internal network thread) — no dedicated event loop or `run_coroutine_threadsafe`/futures dance is needed to call them from `comandos.py`, `visualizacao_remota.py`, or `transferencia_arquivos.py`. Don't reintroduce that complexity if extending this package.
     - Routing is envelope-based, not topic-based: every machine subscribes to the same three fixed shared topics (`jarvis/comandos` for JSON command/response envelopes, `jarvis/frames` and `jarvis/arquivos` for binary payloads with MQTT5 user-properties as metadata), and `TOKEN_REDE_JARVIS` + a `destino` field (checked against `NOME_MAQUINA`/`"todos"`) inside each message decide what gets processed — mirrors the very first (pre-Telegram-bug) design, since that filtering logic was always transport-agnostic.
     - Presence/online-status (`listar_maquinas_remotas`) is the one exception to shared topics: each machine has its own `jarvis/presenca/<NOME_MAQUINA>` topic (subscribed to in aggregate via the `jarvis/presenca/+` wildcard), and uses two MQTT-native mechanisms instead of a request/response round trip — a **retained** "online" message published right after connecting (so any machine that subscribes later still immediately gets everyone's last-known status, no polling needed), and a **Last Will** (`will_set`, configured before `connect()`) that the *broker itself* publishes as a retained "offline" message if a client's connection drops without a clean MQTT disconnect (crash, killed process, network loss — detected via the `keepalive` window, 60s here, so worst-case detection lag is ~1.5×that). `mqtt_listener.py` keeps an in-memory `_MAQUINAS_ONLINE` dict updated purely from these messages; `listar_maquinas_online()` never makes a network call, it just reads that dict. Verified for real against HiveMQ Cloud, including forcibly killing a raw socket (no clean disconnect) and confirming the broker published the Will and the peer's listener removed it from the list within the keepalive window.
     - `win11toast` (WinRT) **must never be imported at module load time** anywhere in this package — importing it before PySide6 has initialized COM in the same process reliably segfaults the interpreter. `notificacoes.py` imports it lazily inside each function for this reason; don't hoist that import to the top of the file.
     - `transferencia_arquivos.py`'s save-file dialog (`QFileDialog`, needed on the receiving end) runs on a background thread but must execute on the Qt GUI thread — it uses a `QObject` with a queued-connection `Signal` (`_PonteSalvarArquivo`) instantiated once, eagerly, from `GeminiLiveWorker.__init__` (i.e. the GUI thread) via `preparar_ponte_gui()`, so its thread affinity is correct.
     - `comandos.py`'s `capturar_tela` has no "just look at it in the chat" option the way Telegram did — the captured frame is delivered through the exact same path as a received file (`jarvis/arquivos`, notification + `QFileDialog`/timeout-to-`PASTA_TRANSFERENCIAS_PADRAO`), not injected into any Gemini session.
     - `visualizacao_remota.py` still runs its own dedicated background event loop (for the `MonitorTelaContinuo` capture cycle, independent of MQTT) — when scheduling a coroutine onto it via `run_coroutine_threadsafe`, never call `loop.stop()` as the coroutine's own last statement — the wrapping `concurrent.futures.Future`'s result-callback can lose the race against the loop stopping, causing the caller's `future.result()` to time out even though the coroutine finished fine. Stop the loop in a separate `call_soon_threadsafe(loop.stop)` after `future.result()` returns (see `visualizacao_remota.parar`).
     - Large file transfers via Google Drive need the *destination* machine's service-account email before sharing; since only its own credential file is known locally, the sender asks for it over the same MQTT channel (`tipo: "consulta_service_account"`) rather than requiring a manually-maintained machine→email mapping — this keeps "add a new machine later" working without touching other machines' config.
   - `jarvis/pacotes/casa_inteligente/` — smart home device control (Tuya, via the official `tuya-connector-python` SDK). Integrated with `jarvis/gemini/cliente_live.py` through the exact same package contract as `rede_jarvis` (`obter_function_declarations()`/`despachar()` — see **docs/INTEGRATION.md**), but with *no* extra session-glue wiring: every action is a one-shot HTTP call, no callbacks or background state needed. `tuya_client.py` is a thin `TuyaOpenAPI` wrapper (auth, generic `get`/`post`, and `enviar_comando(device_id, comandos)` which always returns `(sucesso, mensagem)` — never raises); `dispositivos_tuya.py` holds device discovery/resolution and the per-type action functions. Notable points:
     - **No device IDs are hardcoded in `.env` or in code.** `listar_dispositivos()` calls Tuya's `GET /v1.0/users/{uid}/devices` live and caches the result for `config.DURACAO_CACHE_DISPOSITIVOS_SEGUNDOS` (60s) — long enough to avoid hitting the API on every voice command, short enough that a newly-paired device shows up within a minute without restarting ALFRED. `resolver_dispositivo(nome_falado)` fuzzy-matches (accent/case-insensitive, exact match preferred, falls back to substring match) against that cached list; on zero or multiple matches it returns a clear message instead of guessing, listing the ambiguous candidates or the available device names.
     - Interruptor and tomada get identical treatment (both are plain Tuya switches) — `ligar`/`desligar` send DP code `switch_1` (`config.DP_CODE_SWITCH_PADRAO`), **confirmed live** against a real paired device (a 3-gang "KaBuM! Smart Interruptor Inteligente 3", category `kg`) via the Tuya IoT Platform's own device-debug panel, not assumed.
     - Infrared (via the "IR Control Hub Open Service") is structurally implemented but **functionally unverified** — no IR device was paired yet when this was written. `resolver_dispositivo` transparently expands any device in category `wnykq` (Tuya's universal-IR-hub category, confirmed via docs) into one candidate per already-learned remote (`listar_controles_remotos_ir`, `GET /v2.0/infrareds/{infrared_id}/remotes`), and `enviar_tecla_infravermelho` sends `POST /v2.0/infrareds/{id}/remotes/{id}/raw/command` with `{category_id, key, key_id}` — endpoint and body shape confirmed against the official Tuya docs. What's *not* confirmed: the actual key names for on/off (`TECLA_IR_LIGAR`/`TECLA_IR_DESLIGAR` in `dispositivos_tuya.py`, currently `"power_on"`/`"power_off"`) — some Smart-Life-learned remotes only have a single toggle key, not separate on/off. Test and adjust those two constants once a real IR device+remote exists.
     - `TUYA_API_ENDPOINT` must match the Cloud Project's actual Data Center exactly (confirmed, not guessed, via the project's own Overview tab in the Tuya IoT Platform — for this project specifically, `Western America Data Center` → `https://openapi.tuyaus.com`). Getting this wrong doesn't always fail loudly: `TuyaOpenAPI.connect()` can return `success: true` (a valid token) even when pointed at the *wrong* data center for this project, since the auth/token endpoint is shared more broadly than the actual device data — only device-specific calls (`.../devices`, `.../commands`) reveal the mismatch. Don't treat a successful `connect()` as proof the endpoint is right.
     - Every public Tuya API call — even correct endpoint, correct device ID, called directly from the IoT Platform's own device-debug panel working fine in parallel — failed with `code 28841107` (`"No permission. The data center is suspended..."`) on a brand-new Cloud Project for a while after creation, despite IoT Core showing "authorized" and devices already linked via the Smart Life app. Likely a backend propagation delay on Tuya's side for new projects, not a config problem — don't assume a wiring bug in this package if that exact error shows up again on a fresh project; check `code`/`msg` first.
   - `jarvis/pacotes/delegacao_ia/` — delegates one-off text tasks to a faster/cheaper/more-capable LLM provider than doing it inline in the Live session, for cases where that trade-off is worth an extra call. Same package contract as the others (`obter_function_declarations()`/`despachar()`, no session-glue wiring — see docs/INTEGRATION.md), exposed as a single tool `delegar_tarefa(tipo_tarefa, conteudo)` rather than one tool per provider, so the model (not a separate classification call) picks the task type from conversation context. `roteador.py` splits into two paths, deliberately asymmetric because OpenAI is the most expensive provider in use and is meant to be called rarely:
     - `pergunta_rapida`→Groq and `resumo`→Cerebras go through the generic path (`MAPA_PROVEDOR_PRINCIPAL`/`MAPA_PROVEDOR_FALLBACK`): each has a one-shot fallback to the other cheap/fast provider, and if both fail, `delegar()` returns `MENSAGEM_INDISPONIVEL` — an instruction for the model to just answer itself, failure unmentioned to the user (deliberately baked into the returned tool-result text, not left to system-prompt discipline alone).
     - `segunda_opiniao`→OpenAI is handled separately by `_delegar_segunda_opiniao()`, on purpose with **no fallback** to Groq/Cerebras (falling back would defeat the point of an *independent* second AI) and a **distinct failure message** (`MENSAGEM_SEGUNDA_OPINIAO_INDISPONIVEL`) that explicitly instructs the model to tell the user it couldn't get a second opinion this time, unlike the silent-failure behavior of the generic path. On success, the OpenAI response is wrapped (not returned raw) with an instruction telling the model to compare it against its own reasoning and synthesize both viewpoints rather than relay it verbatim. `instrucao_sistema` in `jarvis/gemini/cliente_live.py` (# DELEGAÇÃO DE TAREFAS section) spells out the pergunta-normal-vs-segunda_opiniao distinction with concrete examples (financial/high-stakes decisions → `segunda_opiniao`; direct-knowledge or no-real-risk reasoning tasks like explaining a concept or comparing two technologies → answer directly, no delegation at all). There used to be a third type, `raciocinio_complexo`→OpenAI — removed entirely; that kind of reasoning is now expected to be handled by Gemini itself rather than delegated by default.
     - `provedores.py` is a single generic OpenAI-compatible-schema HTTP helper (`requests`, no per-provider SDK) reused for all three providers, since Groq/Cerebras/OpenAI all expose the same `POST .../chat/completions` shape; each provider function returns `(sucesso, resultado_ou_erro)`, never raises. **Model names are a moving target** — the ones hardcoded as defaults in `config.py` (`MODELO_GROQ`/`MODELO_CEREBRAS`/`MODELO_OPENAI`, each overridable via its own env var) were confirmed live against each provider's real `/v1/models` endpoint while writing this, not from general knowledge — Groq/Cerebras in particular had already dropped every Llama-branded chat model that used to be the obvious default, in favor of `gpt-oss`/`qwen`/`gemma` families. If a provider call starts failing with a `model_not_found`-shaped error, re-check that endpoint live rather than guessing a replacement name from memory.
   - `jarvis/pacotes/admin_terminal/` — voice-triggered execution of Windows terminal commands with administrator privilege, local to this machine only. Same package contract as the others (`obter_function_declarations()`/`despachar()` — see **docs/INTEGRATION.md**), exposed as two tools: `executar_comando_admin(comando, execucao_longa)` and `confirmar_comando_admin(confirmado)`. Deliberately **not** wired into `rede_jarvis` (not in `jarvis/pacotes/rede_jarvis/comandos.py`'s `TABELA_COMANDOS`, no import in either direction) — remote-triggered admin execution is a separate risk this project hasn't decided on yet.
     - **Elevation without a UAC prompt per command**: a Windows Scheduled Task (name in `config.NOME_TAREFA_AGENDADA`, default `JarvisAdminTerminal`) is created **once per machine**, manually, by running `python -m jarvis.pacotes.admin_terminal.setup` (never called automatically by the app — `setup.py` requires a typed `SIM` confirmation before creating or removing it, and prints exactly what it's about to do). The task is created with RunLevel `HIGHEST` and a dummy one-time trigger already in the past (`/sc once /st 00:00`), so it never fires on its own — it only runs when the app triggers `schtasks /run /tn <nome>`, which does not prompt UAC again (that's inherent to how a Scheduled Task's own configured RunLevel works — the interactive consent prompt only happens at *creation* time, not at `/run` time). `python -m jarvis.pacotes.admin_terminal.setup --remover` deletes the task.
     - **File-based IPC with the elevated process**: `schtasks /run` can't pass a different command string per invocation, so `executor.py` (normal privilege) writes the exact command to `dados/admin_fila/pedido_pendente.json`, triggers the task, and polls for `dados/admin_fila/resultado_pendente.json`. The task's fixed action is `runner_elevado.py` (a separate script — a Scheduled Task can only elevate the process it itself starts, not an already-running one), which runs *only* when invoked this way, reads the pedido file, executes it via `cmd.exe /d /c <comando>` (`/d` disables `cmd.exe` AutoRun-from-registry, so nothing outside the requested command runs), and writes the result back atomically (`.tmp` then `Path.replace`, same convention as `gerenciador.py`).
     - **CRITICAL bug fixed live, reported directly by the user as "mais crítico"**: a command that reads from stdin (the canonical real-world case: `winget upgrade`/`install` prompting to accept a source/package license on first run) hung `runner_elevado.py` indefinitely — no bound at all, past even the command's own `timeout_segundos` — because `subprocess.run([...cmd.exe...], capture_output=True, timeout=...)` didn't set `stdin`, so the child inherited whatever stdin the elevated Scheduled Task process had and blocked waiting for input that could never arrive. Since `executor.py`'s wait for the result file is a genuinely synchronous blocking call running inside the jarvis worker's thread pool (`asyncio.to_thread`), and a blocked OS thread can't be forcibly cancelled from Python, this froze the *entire app* — no response, `encerrar_chamada`/closing the window did nothing, required Task Manager to kill the process. Fixed by switching to `subprocess.Popen(..., stdin=subprocess.DEVNULL, ...)` — any command trying to read stdin now gets immediate EOF and fails fast instead of hanging — plus, on the (now merely defensive, no-longer-load-bearing) `TimeoutExpired` path, `taskkill /F /T /PID <pid>` to kill the whole process tree (a lone `Popen.kill()` only kills the direct `cmd.exe` child, not grandchildren like `winget.exe` it may have spawned, which could otherwise survive as orphans still holding the stdout/stderr pipes open). **Verified live through the real Scheduled Task** (not just a local simulation — a local test via this session's own already-non-interactive shell didn't reproduce the hang, since its stdin was already closed; the elevated task's own console-attached stdin is what actually blocked) — a crafted `set /p x=` request (mimics `winget`'s prompt) resolved in ~2s post-fix through the actual `schtasks /run` → `runner_elevado.py` path, with zero leftover processes, versus hanging indefinitely before.
     - **Second, related bug fixed in the same pass**: the elevated task's own `python.exe` process (launched directly by the Scheduled Task, independent of anything `cmd.exe`-related above) allocates a visible console window on the interactive desktop for its whole lifetime, since `python.exe` is a console-subsystem executable — this is very likely the literal "abre o terminal com python... fica a tela preta" the user described. Fixed in `setup.py`: the task's action now points at `pythonw.exe` (the windowless sibling executable in the same `venv/Scripts` folder, falling back to `python.exe` if somehow absent) instead of `sys.executable`. Safe because `runner_elevado.py` never prints anything meaningful of its own to a console — every outcome, including errors, goes through `_escrever_resultado()` into `resultado_pendente.json` — so switching away from a console-owning interpreter loses no visible information, only the unwanted window. **This half of the fix requires re-running `python -m jarvis.pacotes.admin_terminal.setup` once** (`schtasks /create ... /f` overwrites the existing task's action in place — no need to `--remover` first) — editing `setup.py` alone doesn't retroactively change an already-created task's registered action. The `runner_elevado.py` stdin/taskkill fix above, by contrast, took effect immediately with no task recreation needed, since it's just a change to the script the existing task already points at.
     - **Whitelist matching is literal, never semantic**: `whitelist.py` matches a comando against `whitelist.json` (user-editable, exact-string or prefix+regex entries — e.g. `winget upgrade <pacote>` only accepts `[A-Za-z0-9._-]+` after the prefix) — never an LLM judgment call about whether a command "seems safe". `politica.py` adds two hard overrides on top of whitelist matching, both defense-in-depth beyond what was explicitly requested: (1) any shell chaining/redirection character (`&`, `|`, `;`, backtick, `$(`, `<`, `>`) forces confirmation even if the string's prefix matches a whitelist entry (otherwise `winget upgrade --all & del /f /q C:\` would slip through on the prefix match alone); (2) a pattern suggesting download/execution from a non-official source (`curl`, `wget`, `Invoke-WebRequest`, `.exe`/`.msi`, `Start-Process`) always forces confirmation too, per the original spec's explicit requirement.
     - **Confirmation flow never blocks inside a pending tool call**: unlike `jarvis/pacotes/rede_jarvis/permissoes.py` (which blocks a background MQTT-listener thread — safe because that request never coincides with an outstanding Gemini `tool_call`), `executar_comando_admin` for a non-whitelisted command returns *immediately* with an instruction telling the model to ask the user and then call `confirmar_comando_admin(confirmado)` — this keeps the whole exchange inside normal turn-taking (no function call is ever left outstanding while a spontaneous `send_client_content` is injected, which would be untested behavior against the Live API). A Windows toast notification (Permitir/Negar buttons, `jarvis/pacotes/admin_terminal/notificacoes.py`) is fired in parallel as a second channel; since a toast click resolves in a background thread with no pending tool call to answer, *that* path (and the fail-safe deny on `config.TIMEOUT_CONFIRMACAO_SEGUNDOS` timeout) reports its outcome via the same spontaneous-speech mechanism `rede_jarvis` already uses — see the `_falar_espontaneamente`/`_enviar_anuncio_espontaneo` methods on `GeminiLiveWorker` (renamed from `_falar_rede_jarvis`/`_enviar_anuncio_rede_jarvis` when this became a second consumer; behavior unchanged) and the `admin_terminal` section of docs/INTEGRATION.md.
     - **No status feedback during execution — real UX gap fixed after the stdin-hang/black-window bugs above were resolved**: once those two bugs were fixed, the user reported a third, milder problem — a whitelisted (auto-run) admin command could take up to `TIMEOUT_COMANDO_LONGO_SEGUNDOS` (5 minutes, default) with **zero visible feedback**: no activity-log entry, and (inherently, by the Live API's function-calling protocol — the model literally cannot speak again until it receives the matching `tool_response`) no spoken response either, making a slow-but-working command indistinguishable from a hung one. Package-dispatched tools (`admin_terminal` included) run through the generic `PACOTES_REGISTRADOS` loop in `processar_chamada_de_funcao`, which has no access to `self` from inside `despachar()` — so packages have no way to emit a `status_recebido` update themselves. Fixed the same way `identificar_planta`/`consultar_segunda_opiniao_visual` already special-case their pre-dispatch image capture (same `if nome in (...)` spot, right before the generic dispatch loop): `cliente_live.py` now special-cases `executar_comando_admin`/`confirmar_comando_admin` to emit a `self.status_recebido.emit(...)` (activity log + status label, `jarvis/ui/janela_principal.py:atualizar_status`) both right before dispatch and right after it resolves. This is UI-only — `status_recebido` never touches the Gemini session, so it carries none of the "spontaneous `send_client_content` while a tool_call is pending" risk from the bullet above; it does **not** make the model speak during execution, since that protocol limitation can't be worked around this way — it only makes the wait visibly distinguishable from a freeze. If another package tool ever needs the same visibility (long-running, package-dispatched, no native pre/post hook), copy this same `elif nome in (...)` pattern rather than inventing a new mechanism.
     - **Voice/notification race — real bug reported and fixed**: the two confirmation channels (voice, via `confirmar_comando_admin`, and the Windows toast) are both armed in parallel by the same `solicitar_confirmacao()` call, and `_tomar_pendente()` correctly ensures only the first to respond actually executes anything — but a user naturally does both at once (clicks "Permitir" *and* says "sim"), and whichever channel loses the race used to get told "Não havia nenhum comando administrativo aguardando confirmação" — which reads as "nothing happened," when the command had in fact already run (successfully or not) via the other channel. `dados/logs/comandos_admin.log` had two `CONFIRMADO | OK` entries confirming the notification path really was executing commands correctly; the confusion was purely in what the losing voice call got told. Fixed with a short-TTL (120s) `_ultimo_resolvido` cache in `confirmacao.py`, written by both `responder_confirmacao_por_voz()` and `_resolver_por_notificacao()` whenever either one wins the race — so the loser, finding nothing pending, now checks this cache and reports the *actual* result instead of the dead-end message (falling back to the original generic message only when the cache is empty or stale, i.e. a genuine "nothing was ever pending" case). Verified in isolation: no-cache → generic message; cache populated (simulating the notification winning) → voice call reports the real result; cache past TTL → falls back to generic again.
     - `jarvis/pacotes/admin_terminal/notificacoes.py` is a deliberate, small, standalone duplicate of `jarvis/pacotes/rede_jarvis/notificacoes.py`'s win11toast wrapper (including the same lazy-import-to-avoid-COM-segfault pattern) rather than an import from `rede_jarvis` — keeps the two packages at zero coupling in either direction, matching the "don't connect admin_terminal to rede_jarvis" constraint.
     - Every execution (automatic or confirmed, successful or not) is appended to `dados/logs/comandos_admin.log` via `executor.registrar_log` — timestamp, whether it was automatic or confirmed, success/failure, the exact command, and a truncated result summary. **Capped, not rotated into multiple files, not deleted**: `executor._aparar_log_se_necessario()` runs before every write and, once the file passes `config.LIMITE_TAMANHO_LOG_BYTES` (5MB), discards the oldest half of its lines and keeps the rest — closes a real, previously-flagged "grows forever, nobody notices" gap. `jarvis/pacotes/rede_jarvis/mqtt_listener.py`'s `comandos_remotos.log` got an independent copy of the exact same technique (own `_aparar_log_se_necessario()`, own `config.LIMITE_TAMANHO_LOG_BYTES`), per this project's per-package duplication convention rather than a shared helper.
   - `jarvis/nucleo/` — not a tools package (no `obter_function_declarations()`/`despachar()`), just a tiny generic bridge: `sinalizador.py` holds a `QObject` (`SinalizadorInterfacesExtras`) with one `Signal` per "a background-thread package needs to open a Qt window" use case (currently `solicitou_abrir_configuracoes`), plus `obter_sinalizador()`, a lazily-created (not at import time — only on first call, after `QApplication` exists) module-level singleton. Exists because a `despachar()` call always runs off the GUI thread (`asyncio.to_thread`), but Qt windows can only be created/shown on the GUI thread — a package emits the signal, and the one connection that turns it into an actual window lives in `main.py` (see below), never in `jarvis/ui/janela_principal.py`. Any future package that needs to open a window should add a `Signal` here rather than invent a new threading mechanism.
   - `jarvis/pacotes/configuracoes/` — the settings window: view/edit every configured package's `.env` variables from one voice-triggered screen (`abrir_configuracoes` tool). Same package contract as the others (`obter_function_declarations()`/`despachar()` — see **docs/INTEGRATION.md**); `despachar()` does nothing but `jarvis.nucleo.sinalizador.obter_sinalizador().solicitou_abrir_configuracoes.emit()` — the actual `ConfiguracoesWindow` (`jarvis/pacotes/configuracoes/window.py`) is only ever instantiated by the slot connected in `main.py`.
     - `env_io.py` never rewrites the whole `.env`: `dotenv_values()` (read-only) for showing current values, `set_key(..., quote_mode="never")` (update-one-key-in-place) for saving — both from `python-dotenv` — so comments, ordering, and every other package's variables survive untouched. A variable absent from the file shows as a blank field, not a fake placeholder.
     - **Dynamic discovery via a second contract, not filesystem scanning**: each package that has `.env` variables exposes `config_schema()` on its own `config.py` — a list of `{"nome", "rotulo", "sensivel", "obrigatoria"}` dicts describing exactly the variables that package's `config.py` already reads via `os.getenv(...)` (never invented names). `jarvis/pacotes/configuracoes/pacotes.py`'s `PACOTES_COM_CONFIG` is an explicit registration list (separate from `PACOTES_REGISTRADOS` in `cliente_live.py`, since not every registered tool package necessarily has `.env` config) — adding a package's section to the settings screen means adding `config_schema()` to its `config.py` *and* one line here; nothing in `jarvis/pacotes/configuracoes/window.py` itself changes. Covers every `jarvis/pacotes/` package with `.env` config, plus three non-package modules that also read `.env` and were a previously-flagged, now-closed gap: `jarvis/nucleo/config.py` (`GEMINI_API_KEY`, `EXIGIR_AUTENTICACAO`, `TIMEOUT_INATIVIDADE_SEGUNDOS` — `GEMINI_LIVE_MODEL`/`GEMINI_VOICE` deliberately excluded, since those are fixed Python constants, never read from `.env`) and `jarvis/servicos/email/`'s `remetente.py`/`leitor.py` (registered as two separate sections, "Email — Envio (SMTP)"/"Email — Leitura (IMAP)", since each module independently reads its own `os.getenv(...)` calls — `EMAIL_REMETENTE`/`EMAIL_SENHA_APP` intentionally appear in both, because both modules genuinely read those same two keys independently, not a copy-paste mistake). `PACOTES_COM_CONFIG` doesn't actually require its entries to be `jarvis/pacotes/` modules — any module exposing `config_schema()` works, which is what made adding these three trivial.
     - Sensitive fields (`sensivel: True`) start masked (`QLineEdit.EchoMode.Password`) with a per-field "Mostrar"/"Ocultar" toggle button; never printed/logged anywhere outside the field itself.
     - The window shows a persistent, explicit warning that some changes only take effect after restarting the app (packages that already opened a connection this session — e.g. `rede_jarvis`'s MQTT client — don't hot-reload their config) — deliberately not implying anything is instant when it isn't.
     - Threading note for future packages needing a settings section: `_CampoConfig` in `window.py` only ever runs on the GUI thread (constructed inside `ConfiguracoesWindow.__init__`, itself only ever instantiated by the `main.py` slot) — `config_schema()` itself is plain data (no I/O, no Qt objects), so it's safe to call from anywhere.
   - `jarvis/pacotes/identificacao_planta/` — species identification from a camera photo via Pl@ntNet (`my-api.plantnet.org`), a botany-specialized API, used specifically because species ID isn't a general vision model's strong suit — this tool never touches the Gemini-based general camera analysis (`analisar_camera`), which is untouched and still the default for any other camera question. Same package contract as the others (`obter_function_declarations()`/`despachar()` — see **docs/INTEGRATION.md**), one no-parameter tool, `identificar_planta`.
     - **Request/response shape confirmed against the official docs before writing any code** (`my.plantnet.org/doc/api/identify` and the official example at `github.com/plantnet/my.plantnet/blob/master/examples/post/run.py`), not guessed: `POST https://my-api.plantnet.org/v2/identify/{project}?api-key=...`, image as multipart form field `images`, `organs` deliberately omitted (the docs confirm omitting it sets every image to `auto`-detection, which is the right default for a single unlabeled camera frame). `PROJETO_PLANTNET` defaults to `"all"` (broadest flora search) since the camera has no way to know the plant's region.
     - `plantnet_client.identificar()` returns up to `config.QUANTIDADE_RESULTADOS` (3) candidate species — scientific name, common name(s) if present, confidence score — in the order the API already returns them (descending confidence), never just the top match, specifically so the model can express "acho que é X, mas pode ser Y" instead of false certainty. `instrucao_sistema`'s `# IDENTIFICAÇÃO VISUAL ESPECIALIZADA` section tells the model to communicate uncertainty when the top score isn't clearly ahead of the second one — deliberately a system-prompt judgment call rather than a hardcoded numeric threshold in the tool response, since the model can weigh both the absolute percentage and the gap between candidates.
     - `_mensagem_erro()` gives a distinct message per known HTTP status (401 invalid/expired key, 404 no matching species, 429 daily quota exhausted), falling back to a generic message with whatever `message`/`error` field the JSON body has otherwise — never raises, matching every other package's `(sucesso, resultado)` convention.
     - **Verified against the real API, not just `py_compile`**: tested end-to-end (`plantnet_client.identificar()` and the full `despachar()` path) with a real `PLANTNET_API_KEY` and a real flower photo — correctly returned `Impatiens hawkeri` (New Guinea Impatiens) at 82% confidence as the top candidate, with two lower-confidence alternatives. Also confirmed the 401 (`"Bad token"`) error path with a deliberately invalid key.
   - `jarvis/pacotes/identificacao_visual/` — a second, independent "second opinion" vision flow, added alongside `identificacao_planta` for the *inverse* case: general (non-plant) object identification, where Gemini's own generalist vision can also be wrong. One tool, `consultar_segunda_opiniao_visual(pergunta)`, using **Mistral** (`api.mistral.ai/v1/chat/completions`) as the independent second source — same "don't just repeat the external result" principle as `identificacao_planta`, applied via a shared mechanism (see below).
     - **Pixtral — the model originally requested for this — is deprecated and was never used.** Confirmed live against Mistral's own deprecation docs before writing any code: `pixtral-12b-2409` retired 2025-12-31, `pixtral-large-2411` retired 2026-05-31 (both already past retirement when this was built). Mistral no longer ships a separate dedicated vision model at all — vision is now built into the general Large/Medium/Small lineup. `mistral-medium-latest` (`config.MODELO_MISTRAL_VISION`, overridable via `IDENTIFICACAO_VISUAL_MODELO_MISTRAL`) was a **conscious choice by the user** after being shown this finding — the docs list it as the closest replacement for the old Pixtral Large — not a default picked without checking. If Mistral's catalog shifts again, re-check `docs.mistral.ai`'s model deprecation page live rather than assuming a name still works (same lesson as Groq/Cerebras's model churn in `delegacao_ia`).
     - **Request shape confirmed against the official docs and a working community example** (`docs.mistral.ai/capabilities/vision`, `github.com/eniompw/Mistral/image_base64.py`), not guessed: the image content item is `{"type": "image_url", "image_url": "<string>"}` — the value is a **plain string** (a data URI for a local image: `data:image/jpeg;base64,<b64>`), *not* an OpenAI-style nested `{"url": ...}` object, despite the endpoint otherwise being OpenAI-compatible. Getting this shape wrong wouldn't necessarily fail loudly (worth remembering if this endpoint ever needs revisiting).
     - **`pergunta` is a real Gemini-filled parameter, not omitted** — despite the tool being originally spec'd as parameter-free like `identificar_planta`, passing the user's literal question through to Mistral (rather than a generic fixed prompt) required a `pergunta` string parameter; this was an explicit, conscious tradeoff the user picked when the contradiction was pointed out, not an oversight.
     - `mistral_vision_client.consultar()` treats HTTP 429 as an expected, distinctly-worded case (mentioning the free tier's low requests-per-minute limit), not a generic failure — per the user's explicit request, since a second-opinion feature calling a rate-limited free tier will hit this regularly. Every failure path (missing key, no image, timeout, connection error, 401, 429, other non-200, malformed response) returns text that already instructs the model to answer with its own vision alone and tell the user a second source couldn't be reached — mirrors `delegacao_ia`'s `segunda_opiniao` convention (failure must be mentioned to the user), not the silent-failure convention used by `delegacao_ia`'s `pergunta_rapida`/`resumo`.
     - **Verified against the real API, not just `py_compile`**: tested `mistral_vision_client.consultar()` and the full `despachar()` path with a real `MISTRAL_API_KEY` and a real (non-plant) photo — `mistral-medium-latest` correctly and accurately described a photo of a person harvesting wheat with a sickle. Also confirmed the 401 and no-image failure paths.
     - **Cross-checking mechanism shared with `identificacao_planta`**: neither package's `despachar()` result is presented to the user as-is. `GeminiLiveWorker.enviar_imagem_para_cruzamento(imagem_bytes, resultado_externo, contexto)` (a new generic method, alongside `_enviar_anuncio_espontaneo`) re-sends the *exact same* captured frame to Gemini via `send_client_content` — inline image + a `[SISTEMA]` text naming the external source and its result, instructing the model to look at the image itself and explicitly say whether it agrees or disagrees, never just relaying the external answer as the only opinion. Called from `processar_chamada_de_funcao` right after either tool's dispatch result is obtained, *before* the `tool_response` is sent — the same message ordering already proven safe by the existing `analisar_tela`/`analisar_camera` flow (image arrives via `send_client_content` first, `tool_response` closes out the call after). See the `identificacao_planta` and `identificacao_visual` section of docs/INTEGRATION.md for the exact wiring — this is the second reuse of the "inject client-only data around the generic dispatch loop" pattern (before *and* after the loop, this time) and the reference to follow for any future tool with the same need.
   - `jarvis/pacotes/chat_jarvis/` — two tools, `abrir_chat` and `abrir_envio_arquivo`, opening `jarvis/ui/janela_chat.py`/`jarvis/ui/janela_envio_arquivo.py` — text/file windows connected to the **same** in-progress Live session (not a separate conversation). Same package contract (`despachar()` only emits a signal, mirroring `configuracoes`), plus one real exception: unlike every other signal in `jarvis/nucleo/sinalizador.py`, this feature needs new data to flow *into* the active session from the GUI thread, which needed two new public methods on `GeminiLiveWorker` — see the `chat_jarvis` section of docs/INTEGRATION.md for the full wiring (both directions) and reasoning. Summary:
     - **Chat text / dropped-file content → session**: `GeminiLiveWorker.enviar_texto_da_ui(texto)` / `.enviar_imagem_da_ui(imagem_bytes, mime_type, texto_contexto)` — same `asyncio.run_coroutine_threadsafe(..., self.loop)` bridge technique as `_falar_espontaneamente`, just in the opposite direction (GUI → session instead of session → GUI). Both return `False` (no exception) if `self.loop`/`self.sessao` aren't live, so the caller can tell the user instead of silently losing the message.
     - **⚠ Uses `send_realtime_input`, not `send_client_content`** — deliberately different from every other image/text-injection method in this file. Confirmed live against the official Live API docs before writing this: for the model actually configured here (`GEMINI_LIVE_MODEL = "gemini-3.1-flash-live-preview"` in `jarvis/nucleo/config.py`), `send_client_content` is documented as supported **only for seeding a session's initial history**, not for updates mid-conversation — *"To send text updates during the conversation, use `send_realtime_input` instead."* (This restriction is specific to 3.1 Flash; `gemini-2.5-flash-native-audio-preview`, also listed as an option in `jarvis/nucleo/config.py`, documents `send_client_content` as fully supported throughout the conversation.) `send_realtime_input(text=...)` and `send_realtime_input(video=types.Blob(...))` (the latter already the exact mechanism `_injetar_frame_remoto` uses for remote frames) are the two calls used here.
     - **This finding does NOT change any existing code.** `enviar_camera_para_gemini`, `enviar_tela_para_gemini`, `_enviar_anuncio_espontaneo`, and `enviar_imagem_para_cruzamento` all still call `send_client_content` mid-session on this same 3.1 Flash model, and have all been tested working in real calls throughout this project's development. That's a genuine discrepancy between documented behavior and observed behavior worth being aware of — flagged here and left alone by explicit user decision, not fixed opportunistically as a side effect of this feature. If `send_client_content` mid-session ever starts behaving unreliably (dropped context, ignored turns, silent no-ops), this is the first thing to check, and migrating those call sites to `send_realtime_input` would be the fix — but that's a deliberate, separate task, not something to do reflexively while touching this file for something else.
     - **Response text for the chat window**: `response_modalities=["AUDIO"]` alone carries no text — `LiveConnectConfig` needed `output_audio_transcription=types.AudioTranscriptionConfig()` added (confirmed as a real field in the installed `google-genai` SDK before using it, not assumed). `receber_audio` accumulates `resposta.server_content.output_transcription.text` fragments across a turn and emits the full accumulated text exactly once, on `resposta.server_content.turn_complete` — not fragment-by-fragment — via `jarvis.nucleo.sinalizador.resposta_texto_recebida`.
     - **Outgoing signal routed through the shared sinalizador, not a per-worker Signal**: `resposta_texto_recebida` lives on `SinalizadorInterfacesExtras` (a single persistent object) rather than on `GeminiLiveWorker` itself, specifically because `GeminiLiveWorker` is discarded and recreated every call (`MainWindow.live_worker` — see below) — a per-instance Signal would need reconnecting on every new call while the chat window is open; the shared sinalizador doesn't.
     - **How the windows find "the current worker"**: `ChatWindow`/`EnvioArquivoWindow` never hold a `GeminiLiveWorker` reference directly — `main.py` passes them a getter (`_obter_worker_ativo`) that re-reads `MainWindow.live_worker` (already a public attribute — no edit to `jarvis/ui/janela_principal.py` needed) fresh on every call, so the windows keep working correctly across a call ending and a new one starting while they're still open. `main.py` keeps a module-level reference to the `MainWindow` instance created in `main()` purely to make this getter possible.
     - `jarvis/ui/janela_envio_arquivo.py`'s `processar_arquivo_para_sessao(caminho, worker)` is shared (imported) by `jarvis/ui/janela_chat.py`'s drag-and-drop handler, not duplicated — returns `(sucesso, mensagem)`, matching the project-wide convention. Images (`image/jpeg`/`image/png`) go through `enviar_imagem_da_ui`; PDF (`pypdf`, confirmed the Live API does **not** accept PDF as inline media before choosing this fallback — text-only, image-only, and video-only mime types are documented, no document type) and plain-text extensions (`.txt`/`.md`/`.csv`/`.json`/`.log`) have their text extracted and sent via `enviar_texto_da_ui` wrapped in a `[SISTEMA]` prefix (truncated at `_LIMITE_CARACTERES_TEXTO` = 12000 chars, with a note appended if truncated); anything else returns a clear "not supported yet" message — never a silent no-op.
   - `jarvis/pacotes/explorador_windows/` — discovers the file(s) currently selected in whichever Windows Explorer window is in the foreground (`win32com.client.Dispatch("Shell.Application")`, matching each open Explorer window's `.HWND` against `win32gui.GetForegroundWindow()`, then reading `.Document.SelectedItems()`), used by the `preparar_email` *native* tool's "attach the file I just selected" flow (resolved once, at prepare time, and the resolved path is stored on the pending draft — not re-resolved at confirm time). Windows-only (`pywin32`).
     - **Not a tools package in practice, even though it follows the contract shape**: `obter_function_declarations()` returns `[]` and `despachar()` always returns `None` — there's no voice-invokable capability here on its own (asking "what file is selected?" as an end in itself doesn't make sense; it's always a step inside another tool). Deliberately **not** added to `PACOTES_REGISTRADOS` for exactly that reason — doing so would just add a permanently-`None` `despachar()` call to every single tool dispatch in the app, for a package that never intercepts anything. `obter_arquivo_selecionado()` is called directly, the same way `capturar_camera_bytes()`/`capturar_tela_bytes()` already are.
     - **Consumed by a native tool, not a package** — unlike `identificacao_planta`/`identificacao_visual` (whose "capture-before-dispatch" special case lives around the generic `PACOTES_REGISTRADOS` loop), `preparar_email` is defined in `function_declarations_nativas` and dispatched in the native `elif` chain, so its use of `explorador_windows.obter_arquivo_selecionado()` lives entirely inside that one `elif nome == "preparar_email":` branch — no dispatch-loop changes needed for this one.
     - **Never guesses when ambiguous**: 0 selected items or 0 foreground Explorer windows → returns `sucesso=False` with a specific reason (never falls back to "just pick something"); more than 1 selected item → returns `sucesso=True` with *every* path, and the `preparar_email` branch explicitly refuses to auto-pick the first one — it aborts (no pending draft is created) and tells the model to ask the user which file, mirroring the disambiguation pattern already established in `memory_manager.esquecer_memoria` and `casa_inteligente.resolver_dispositivo`.
     - **Verified against a real, actively-selected file in Explorer**, not just code review — see `jarvis/servicos/email/remetente.py`'s note above for the same end-to-end test this package was part of.
   - `jarvis/pacotes/abrir_app_local/` — **REMOVED.** Replaced by `jarvis/pacotes/abrir_aplicativo/` (see the "Packages brought over from `JARVIS COMPLETO`" section below). Its `PASTAS_EXTRAS_APPS` support was carried over; its `dados/apps_conhecidos.json` cache and its `difflib`-based fuzzy matching were not (the replacement resolves names through Windows' own aliases and the Start Menu instead). The old code is still in git history if any of it is ever needed back.
   - `jarvis/pacotes/discord_jarvis/` — sends a Discord DM to a friend by spoken name (`enviar_dm_discord(nome_amigo, texto)`) and a message to a text channel (`enviar_mensagem_discord(canal, texto)` — `canal` optional, auto-uses the single already-known channel if exactly one is cached, otherwise asks). Built entirely from scratch in one session — despite the request initially describing it (and, in a follow-up within the same task, the channel-message feature specifically) as already existing from "an earlier prompt in this conversation," no trace of any Discord code or discussion existed anywhere in the repo or session history before this; flagged to the user both times rather than silently inventing continuity with nonexistent prior work.
     - **`discord.py` needs a permanently-live `asyncio` loop, unlike every synchronous-transport package before it** — `cliente.py` runs `cliente.start(token)` in a dedicated background thread with its own event loop (same "dedicated background loop" pattern already used by `jarvis/pacotes/rede_jarvis/visualizacao_remota.py`, not a new mechanism), and bridges synchronous calls from `despachar()` in via `asyncio.run_coroutine_threadsafe(coro, loop).result(timeout=...)`. `iniciar_discord_jarvis()` is called once from `GeminiLiveWorker.__init__`, idempotent, same pattern as `rede_jarvis.iniciar_rede_jarvis`/`admin_terminal.iniciar_admin_terminal` — the connection persists across the worker being recreated every call.
     - **Setup requires two Privileged Gateway Intents enabled in the Developer Portal** (`Server Members Intent`, `Message Content Intent`), on top of `DISCORD_BOT_TOKEN` in `.env` — confirmed live, not assumed: omitting either makes the connection fail explicitly (`discord.errors.PrivilegedIntentsRequired`) rather than degrading silently, and `Server Members Intent` specifically is what `Guild.fetch_members()` (used for member search) depends on. The bot also has to actually be invited to a server the target person is in — `buscar_membro`/`buscar_canal` only ever see servers the bot shares with them.
     - **Member/channel resolution mirrors the project's usual name-matching pattern** (accent/case-insensitive normalize, exact → substring → `difflib` fallback) but is a fresh, independent copy in `jarvis/pacotes/discord_jarvis/contatos.py`/`canais.py`, not an import — consistent with every other package in this project keeping its own `_normalizar`, deliberately, rather than sharing logic across package boundaries. Both search fresh via `Guild.fetch_members(limit=None)`/`guild.text_channels` each time (no long-lived unresolved-name cache — only individually *confirmed* resolutions get cached), avoiding the member-cache-population race condition `discord.py`'s own docs warn about (`Guild.members` isn't reliably populated right after connecting even with the intent on).
     - Disambiguation shows `@username` rather than the legacy `#1234` discriminator tag — Discord's username system no longer assigns discriminators to most accounts, so `@username` (unique per account) is the current stable way to tell two same-named people apart, not the old tag digits the original request assumed.
     - `cache_contatos.py`/`cache_canais.py` are separate files with separate JSON stores (`discord_contatos.json`, `discord_canais.json`, both gitignored — contain real Discord user/channel IDs), each independently mirroring `gerenciador.py`'s atomic-write technique — not shared with each other, matching this project's per-package (here, even per-*kind-of-cached-entity*) cache isolation convention. Both write only after a real, successful send.
     - **Verified against a real bot, a real server, and real people — not mocked**: connected live (after walking through enabling both intents from a real `PrivilegedIntentsRequired` failure the first attempt), listed real guild members and channels, resolved "Luan" via a real username substring match (`Japaa._.` / `@luansugano` — display name had no "luan" in it, only the username did), sent a real DM and a real channel message with content the user explicitly approved first, confirmed both writes to their respective caches only after the send succeeded, and confirmed a repeat DM to the same name used the cache (~0.5s, no member listing) instead of searching again.
   - **Capture (print/foto) send unification** — `enviar_captura_email` / `enviar_captura_discord_dm` / `enviar_captura_remoto`, all defined and dispatched natively in `jarvis/gemini/cliente_live.py` (not a package), unify `salvar_print_tela` AND `tirar_foto_camera` with three already-existing send mechanisms without duplicating any of their logic. Originally built print-only (`enviar_print_email`/`enviar_print_discord_dm`/`enviar_print_remoto`, `self.ultimo_print_caminho`/`ultimo_print_timestamp`), then generalized in a follow-up task to also cover webcam photos — renamed throughout, since "envie isso" needs to mean whichever of the two was captured most recently, not just prints. `GeminiLiveWorker` holds a single shared "last capture" reference (`self.ultima_captura_caminho` / `self.ultima_captura_timestamp`, set by `salvar_print_tela`'s and `tirar_foto_camera`'s own branches and by the helper below), reused across all five capture-producing tools so "envie isso"/"envie este print"/"envie essa foto" right after any of them picks up the same file instead of capturing twice.
     - `_obter_ou_capturar_ultima_captura(forcar_captura_nova=False, tipo_captura=None)` is the single reuse-or-capture helper all three sending tools call: if `self.ultima_captura_caminho` exists and is within `TIMEOUT_ULTIMA_CAPTURA_SEGUNDOS` (300s, same naming/lazy-check convention as `TIMEOUT_RASCUNHO_EMAIL`/`TIMEOUT_VISUALIZACAO_CONTINUA`) and the caller didn't force a fresh capture, it's reused as-is (whichever type it is — the caller never needs to know); otherwise it needs `tipo_captura` ("print" or "foto") to know what to capture fresh — via the existing `capturar_monitor_do_cursor_bytes()`+`salvar_print_bytes()` or `capturar_camera_bytes()`+`salvar_foto_bytes()` (same functions `salvar_print_tela`/`tirar_foto_camera` themselves use — no reimplementation) — and updates the shared reference. **Ambiguity is handled in code, not just prompt discipline**: if a fresh capture is needed (no reusable one, or `forcar_captura_nova=True`) and `tipo_captura` is missing/invalid, the helper captures nothing and returns a text asking the model to clarify print-vs-foto with the user before calling again — it never guesses. The three tools each expose `capturar_novo` (boolean, "tire um print/uma foto e envie" → `true`; "envie este print"/"essa foto"/"isso" → `false`/omitted) and `tipo_captura` (only needed alongside `capturar_novo=true` or when no recent capture exists) — see the `instrucao_sistema` "ENVIO DE CAPTURA (PRINT OU FOTO)" section.
     - `enviar_captura_email` never sends directly — it calls `_preparar_rascunho_email(destinatario, assunto, corpo, caminho_anexo)`, extracted from `preparar_email`'s own dispatch branch (behavior-preserving refactor; `preparar_email` itself now just calls this same helper), so the capture goes through the exact same code-enforced `preparar_email`/`confirmar_envio_email` two-step confirmation as any other email attachment — there is no separate, unconfirmed send path for captures. Default `assunto`/`corpo` text differs by type ("Print de tela"/"Foto da câmera") when the user doesn't specify either.
     - `enviar_captura_discord_dm` calls `discord_jarvis.enviar_dm_discord()` directly (not through `despachar()`, since `caminho_anexo` isn't a Gemini-controlled parameter), reusing its existing contact resolution and cache exactly as `enviar_dm_discord`'s own voice tool does. This required extending `enviar_dm_discord(nome_amigo, texto, caminho_anexo=None)` with one new optional trailing parameter (both `cliente.enviar_dm()` call sites already supported an attachment internally) — the voice-facing `FunctionDeclaration` for `enviar_dm_discord` itself is untouched, since ordinary DMs still don't take an attachment from the model.
     - **`enviar_captura_discord_canal` — added later, filling a real gap the user hit live**: asked jarvis to send a screenshot to a Discord *channel* (not a DM), and since no tool existed for that, the model fell back to `enviar_mensagem_discord` (text-only) and just said "print da tela" as filler text with no image ever attached — confusing, not a crash, but a genuine missing capability, not a bug in the existing tools. Fixed the same way `enviar_captura_discord_dm` was: `discord_jarvis.enviar_mensagem_discord(canal_falado, texto, caminho_anexo=None)` gained the same trailing optional parameter `enviar_dm_discord` already had — `cliente.enviar_mensagem_canal()` already supported an attachment at the lowest layer the whole time, it just never had a caller passing one through. The new native tool mirrors `enviar_captura_discord_dm`'s shape (same `capturar_novo`/`tipo_captura` params, same `_obter_ou_capturar_ultima_captura` reuse-or-capture call, same "capturei uma nova imagem agora" prefix), but `canal` is optional (unlike `nome_amigo`, which is required) — empty means the dispatch decides on its own, exactly like the voice-facing `enviar_mensagem_discord` tool already does (single known channel → use it; multiple → ask; none → ask for a name). `instrucao_sistema`'s "ENVIO DE CAPTURA" section was updated from "três tools" to "quatro", with an explicit rule telling the model to use this one instead of the DM version when the user names a channel rather than a person, and vice versa.
     - `enviar_captura_remoto` calls `rede_jarvis.enviar_comando_remoto(maquina_destino, "enviar_arquivo", {"caminho": caminho_captura})` directly — the exact same `enviar_arquivo` command `rede_jarvis` already handles locally (see its own section above), so remote delivery (MQTT direct or Google Drive fallback for large files) is 100% reused, not reimplemented.
     - **Verified against real photo capture and a real Discord DM with a photo attachment** (a genuinely new combination — the first end-to-end test sending a `tirar_foto_camera` output through `enviar_captura_discord_dm`), and against the print-vs-foto disambiguation: captured a print right after an older photo was the "last capture," then confirmed "envie isso" (no `tipo_captura`) reused the print (the more recent one), not the photo — and separately confirmed that with zero recent captures and no `tipo_captura` given, the helper asks instead of guessing.
   - `jarvis/pacotes/camera_preview/` — a live webcam preview window (`abrir_camera`/`fechar_camera`, `jarvis/ui/janela_camera.py`'s `JanelaCamera`), distinct from the single-frame `analisar_camera`/`tirar_foto_camera`. Same signal-emitting package contract as `configuracoes` (`despachar()` only emits, the window is created/closed on the GUI thread from `main.py`), plus the same `ao_fechar` callback pattern `ChatWindow`/`EnvioArquivoWindow` already use, since this window can also close itself (X button, not just voice) and `main.py` needs to know when that happens. Opening while already open focuses the existing window (`raise_()`/`activateWindow()`) instead of creating a second one.
     - **Device-concurrency risk confirmed live before writing any code, not assumed**: most webcams (tested here against the OpenCV MSMF backend, the Windows default) don't error when a second `cv2.VideoCapture(0)` opens while a first is already open, but the second handle measurably interferes with the first while both coexist — a real test showed the already-open handle losing 100% of its frames for the entire duration a second handle stayed open, recovering immediately once the second was released. Because of this, `JanelaCamera` and `vision.camera_capture.capturar_camera_bytes()` (used by `analisar_camera`, `tirar_foto_camera`, `identificar_planta`, `consultar_segunda_opiniao_visual`) **share the same camera handle** instead of each opening their own.
     - `jarvis/servicos/visao/captura_camera.py` gained a small shared-handle API: `abrir_camera_compartilhada()` / `fechar_camera_compartilhada()` / `camera_compartilhada_esta_aberta()` / `ler_frame_camera_compartilhada()`, guarded by a `threading.Lock` (protects the preview's `QTimer` reads on the GUI thread against a concurrent point-in-time capture on the Gemini worker thread). `JanelaCamera` owns the shared handle's entire lifecycle (opens it in `__init__`, releases it in `closeEvent`) — `capturar_camera_bytes()` never opens or closes it, only reads from it when `camera_compartilhada_esta_aberta()` is `True`; when it's `False` (the normal case, preview closed), `capturar_camera_bytes()`'s original behavior — open its own `cv2.VideoCapture(0)`, warm up, read, release — is completely unchanged.
     - **Verified against real concurrent use, not just code review**: opened the live preview, confirmed the shared handle streams with zero read failures; called `capturar_camera_bytes()` twice while the preview stayed open (simulating "tire uma foto" and "analise a câmera" back to back) — both succeeded immediately with no hang, and the preview kept reading with zero errors afterward (the earlier two-separate-handles test, for comparison, showed 37 consecutive dropped frames on the preview side during a single concurrent capture). Also confirmed closing the window truly releases the device — reopened a fresh `cv2.VideoCapture(0)` immediately after close and read a frame successfully — and confirmed that requesting to open the camera while already open reuses the same `JanelaCamera` instance instead of creating a second one.
   - `jarvis/pacotes/navegador_jarvis/` — **REMOVED.** Replaced by `jarvis/pacotes/navegador_web/` (see the "Packages brought over from `JARVIS COMPLETO`" section below). It drove a real Chromium through Playwright's async API in a persistent session, which is what made `pausar_musica`/`retomar_musica` possible; those two tools are gone with it, and `playwright` came out of `requirements.txt`. If browser *control* (as opposed to just opening a URL) is ever wanted again, that package's design — dedicated background thread with its own loop, self-healing session, `video.paused` checked before pressing "k" — is worth recovering from git history rather than redesigning.
   - `jarvis/pacotes/ativacao_voz/` — wake-word call activation ("say the word, the call starts by itself") plus a separate, unrelated-in-code inactivity auto-hangup, requested together but living in different places. **Not a tools package** (same case as `explorador_windows`): `obter_function_declarations()` returns `[]`, `despachar()` always returns `None`, not in `PACOTES_REGISTRADOS` — there's no sense in a tool that's callable only *during* a session for a mechanism whose whole point is deciding when a session *starts*. Used two ways instead: `main.py` calls `ativacao_voz.iniciar(callback_ativacao=...)` once, in `main()`, after the main window exists; `GeminiLiveWorker` calls `ativacao_voz.pausar()`/`ativacao_voz.retomar()` directly around its own `executar()` lifecycle (`pausar()` as literally the first line of `executar()`, `retomar()` right after `self.sessao = None` at the end) — the same kind of direct, non-`despachar()` call `rede_jarvis.iniciar_rede_jarvis()`/`admin_terminal.iniciar_admin_terminal()` already make from `__init__`, just at two lifecycle points instead of one.
     - **Vosk, not Picovoice Porcupine — a deliberate pivot mid-task, not the original design.** Porcupine (dedicated wake-word engine) was built first, confirmed live to still require a free Picovoice Console AccessKey — the user then explicitly asked for a no-account alternative ("só com python mesmo, reconhecendo o que foi falado e comparando com o nome"), so the whole detector was rewritten around `vosk` (package `vosk`) instead: generic offline speech-to-text, zero accounts/keys, `Model(lang="pt")` auto-downloads a small Portuguese model (~31MB, confirmed live) from alphacephei.com on first use and caches it locally — no signup anywhere in the flow. Trade-off, stated honestly to the user: generic STT is less accurate at spotting one specific word than a purpose-built wake-word model, so false negatives (especially words run together with no pause) are more likely than Porcupine would have had.
     - **Model loading happens inside the detection thread itself (`_loop_deteccao`), not in `iniciar()`/`_abrir()`**, specifically so `main.py`'s `main()` never blocks app startup on a potentially slow first-time model download. `_modelo` (the loaded `vosk.Model`, heavy) is a module-level singleton loaded once and reused across every `pausar()`/`retomar()` cycle; only the lightweight `KaldiRecognizer` and the `sounddevice` stream are recreated per cycle. `pausar()`'s `thread.join()` uses a generous 30s timeout specifically to cover the rare case where a call is manually started while the first-ever model download is still in flight — every run after the first, the model is already cached and this resolves near-instantly (confirmed live, ~0.1s).
     - **Detection**: `reconhecedor.AcceptWaveform()`/`PartialResult()`/`Result()` return JSON strings (confirmed live against the actual `vosk-api` source, not just prose docs) — `PartialResult()`'s `"partial"` text is checked on every block (not just finalized `Result()`'s `"text"`) for lower latency, since Vosk only finalizes an utterance on a pause. Matching (`_contem_palavra_ativacao`) normalizes accents/case (same `_normalizar` pattern independently copied from `fechar_app`/`discord_jarvis`, not imported), tries the whole phrase as a contiguous substring first, and — for a multi-word phrase — falls back to requiring every target word present *somewhere* in the recognized text (not necessarily adjacent, via `_palavra_esta_presente`, itself falling back to `difflib.get_close_matches` at the same 0.72 cutoff used elsewhere in the project) so a natural insertion like an article ("iniciar **a** chamada") still matches a contiguous-substring target ("iniciar chamada").
     - **`"jarvis"` was the original default and is fundamentally broken — a real bug found live during testing, not a tuning issue.** The small Portuguese Vosk model has a *closed* vocabulary (a fixed decoding graph, `HCLr.fst`/`Gr.fst`); a word absent from it can never be recognized, no matter how clearly spoken, confirmed two ways: `Model.vosk_model_find_word("jarvis")` returns not-found, and even Vosk's grammar-constrained mode (`KaldiRecognizer(model, rate, grammar_json)`, intended for exactly this kind of small-fixed-vocabulary matching) logs `Ignoring word missing in vocabulary: 'jarvis'` and silently drops it. A live mic test confirmed the failure mode exactly: the model kept substituting unrelated in-vocabulary words ("games", "jogos", "adicione") instead of ever producing anything close to "jarvis". The default was changed to **`"iniciar chamada"`** — both words confirmed present in the vocabulary before committing to it — and `config_schema()`'s label now says this constraint explicitly. Any future default (or anyone picking their own `NOME_ATIVACAO`) must be checked against the model's real vocabulary the same way, not assumed to work because it seems like a normal word.
     - **Device-concurrency handling mirrors `camera_preview`'s, applied to the microphone**: the detector and `GeminiLiveWorker.enviar_microfone()` can never hold the mic at the same time. `pausar()` is synchronous and blocks (`thread.join()`) until the detector's stream is genuinely closed before returning — called as the very first statement of `executar()`, guaranteeing the mic is free before `enviar_microfone` tries to open it.
     - **`sounddevice`, not `pvrecorder`/PyAudio**: reused via a **blocking** read (`stream.read(TAMANHO_BLOCO_VOSK)`, confirmed working with a real test before writing the detector) rather than the callback-based mode `enviar_microfone` uses, since the detector's own thread has no `asyncio` loop to interoperate with.
     - **Graceful degradation, not a crash, on any failure** (model load, microphone open): `ativacao_voz.iniciar()` always returns without raising — a failure prints a clear message and the thread exits cleanly via the same `finally` cleanup used for a normal stop, leaving `esta_ativo()` `False`. Verified live: real detector start/stop/restart cycle (`iniciar()` → `esta_ativo()` → `pausar()` → `retomar()`), and a full app launch with zero setup needed (no `.env` variables at all) confirmed clean, no crash, no traceback.
     - **Inactivity auto-hangup** (`TIMEOUT_INATIVIDADE_SEGUNDOS`, read in `jarvis/nucleo/config.py`) lives entirely in `jarvis/gemini/cliente_live.py`, sharing no code with the detector beyond having been requested in the same task: `GeminiLiveWorker.verificar_inatividade()` runs as one more of `executar()`'s concurrent tasks, polling `self.timestamp_ultima_atividade` — updated only when `resposta.data` (the assistant actually speaking) arrives or a `tool_call` is processed, deliberately never by raw microphone audio, so background noise can't reset the timer. On expiry it reuses `_enviar_anuncio_espontaneo()` (the same spontaneous-speech mechanism `rede_jarvis`/`admin_terminal` already use) to warn by voice, then reuses `encerrar_apos_resposta()` (the exact task the `encerrar_chamada` tool already schedules) to actually hang up — no parallel shutdown path was created.
   - **Optional auth gate** — `EXIGIR_AUTENTICACAO` (`jarvis/nucleo/config.py`, default `true`, only `false` disables it), a purely `instrucao_sistema` change in `jarvis/gemini/cliente_live.py`. The AUTENTICAÇÃO block (the "Coisa" keyword gate) was extracted into a separate `bloco_autenticacao` string, conditionally `""` when the flag is off, then spliced into `instrucao_sistema` (`bloco_autenticacao + "Seu nome é ALFRED. " ...`) — this is the *sanctioned* way to disable the gate (an explicit, default-safe `.env` opt-out), not a weakening of the gate's own logic or wording, which is untouched.
     - **Found and fixed a real, unrelated bug while touching this exact code**: two lines at the very start of the (pre-existing) AUTENTICAÇÃO block were missing their trailing space — the project's `"..." "..." ` adjacent-string-literal-concatenation convention requires every segment to end in a space, and these two didn't, jamming words together into `"...palavra chaveFale apenas em português do Brasil.A palavra-chave secreta..."` right at the very start of the most important section of the prompt. This was reported by the user as "parou de pedir a palavra-chave" and initially suspected to be a prompt-too-large/model-forgetting issue — it wasn't; the garbled text was the actual cause, confirmed by fixing it and the user retesting successfully. The whole ~21k-character prompt was scanned programmatically for the same pattern (a lowercase letter immediately followed by an uppercase letter, the signature of this kind of jam) before and after the fix — the only other hits were false positives from `OpenAI`/`Pl@ntNet` having a capital mid-word legitimately.
   - **Local "call started" beep, replacing a model-spoken greeting that turned out too slow** — a fixed "Chamada iniciada." opening line (instructing the model to say it as its very first utterance) was tried first, spliced in via `instrucao_sistema`. Reported by the user as still too slow in practice ("ele está demorando muito para responder 'Chamada iniciada'") even though the activity log showed the call had already started — because that line depended on a full round-trip to Gemini plus the server-side voice-activity-detection deciding the (silent, mic-only) turn had "ended" before the model would generate and speak anything, which routinely took several seconds. Replaced entirely, per explicit user request ("pode colocar um beep no lugar da resposta"), with a local `winsound.Beep(FREQUENCIA_BEEP_CHAMADA_INICIADA, DURACAO_BEEP_CHAMADA_INICIADA_MS)` call (880 Hz, 150 ms) played right after `client.aio.live.connect()` succeeds in `executar()` — before the mic/receive/playback tasks are even created, so it fires as early as the session can possibly signal readiness, with zero dependency on the Gemini round-trip. The "SAUDAÇÃO INICIAL" instruction block was removed from `instrucao_sistema` entirely (not left in as a redundant second signal) since the whole point was to stop waiting on the model for this. `winsound.Beep` is blocking, so it's called via `asyncio.to_thread(...)`, same convention as every other blocking call in this file.
   - **Concurrent function-call execution, replacing a real "one slow tool freezes everything" bug** — `receber_audio`'s `if resposta.tool_call:` handling used to `await self.processar_chamada_de_funcao(sessao, resposta.tool_call)` directly, meaning a single slow or hung function (email, Discord, a long admin command) blocked that whole `async for` loop — no new audio in, no new tool call accepted — until it finished. Confirmed absent from the codebase before this fix (the user described it as already having been discussed and applied in "an earlier task in this conversation" — no trace of that existed anywhere in this session's history, flagged directly rather than assumed). Fixed by never awaiting `processar_chamada_de_funcao` directly again: `receber_audio` now always wraps it as its own `asyncio.Task`, tracked in `self.tarefas_funcao_ativas` (a plain list — safe without a lock since every mutation, including the `Task.add_done_callback` cleanup in `_ao_finalizar_tarefa_funcao`, happens on the single event-loop thread, never a background OS thread), and immediately continues the loop — enabling genuine concurrent execution (e.g. a long terminal command and an email send now run side by side, not queued).
     - **Bounded, not unbounded**: `LIMITE_TAREFAS_FUNCAO_SIMULTANEAS` (4) caps how many tasks can be in flight; the check (`len(self.tarefas_funcao_ativas) >= limite`) happens synchronously in `receber_audio` *before* creating a task — an instant list-length comparison, not a blocking wait, so it can't reintroduce the bug it's next to. Over the limit, the new call gets an immediate rejection response (via the same `_responder_falha_para_lote` helper used for timeouts, see below) telling the model to inform the user and *not* retry automatically — never silently queued, per explicit user requirement.
     - **`_executar_chamada_de_funcao_com_timeout`** wraps the real call in `asyncio.wait_for(..., timeout=...)`. On `asyncio.TimeoutError` *or* any other unhandled `Exception`, it sends an error `FunctionResponse` for every `chamada` in that `tool_call.function_calls` batch via `_responder_falha_para_lote(sessao, tool_call, gerar_mensagem)` — safe to call unconditionally because `processar_chamada_de_funcao` only ever sends its own `send_tool_response` once, at the very end, after its whole internal loop over `function_calls` completes; a timeout/exception partway through means that internal send never happened, so there's no risk of double-responding to the same call id. Never retries automatically — the returned text explicitly instructs the model not to re-invoke the failed function on its own, only if the user asks again by voice, per explicit user requirement. `asyncio.CancelledError` (raised when the *whole call* ends and `executar()`'s cleanup loop cancels every task in `self.tarefas_funcao_ativas`) is deliberately not caught by either `except` clause — it isn't an `Exception` subclass since Python 3.8, so it propagates and correctly marks the task as cancelled instead of being misreported as a generic failure.
     - **Timeout value is per-function, not flat, and this matters for a real reason**: the flat default is `TIMEOUT_TAREFA_FUNCAO_SEGUNDOS` (20s), but `executar_comando_admin`/`confirmar_comando_admin` are overridden in `TIMEOUTS_TAREFA_FUNCAO_POR_NOME` to `admin_terminal.config.TIMEOUT_COMANDO_LONGO_SEGUNDOS + MARGEM_ESPERA_TAREFA_SEGUNDOS + 15` (325s) — using the flat 20s default on them would have cut off a long (`execucao_longa=true`) admin command *before* its own already-fixed, already-tested internal timeout could ever finish on its own, silently regressing that earlier fix. Caught during implementation, not after — worth checking again if any future package gets its own multi-second/minute internal timeout budget.
     - **A known limitation this task does *not* attempt to fix, same root cause as the earlier admin_terminal hang**: cancelling a task awaiting `asyncio.to_thread(...)` stops *waiting* for the underlying thread, but Python cannot forcibly kill a running thread — so a function whose blocking work genuinely never returns (or a synchronous lock it's holding, e.g. `jarvis/pacotes/admin_terminal/executor.py`'s `_lock_execucao`) keeps the background thread-pool worker occupied for its full original duration even after the user has already been told it timed out. This is an accepted, inherent Python-threading limitation (the same one `stdin=DEVNULL` had to fix *at the source* for admin_terminal specifically, rather than relying on an outer timeout alone) — a generic outer timeout can only guarantee the *user* stops waiting and gets told, never that all underlying work actually stops.
     - **New risk this task made newly possible — since fixed, in a later pass**: before this change, two `tool_call`s could never truly overlap (each one fully awaited before the next was even read from the session), so two camera-capturing tools could never race for the webcam device. Once they genuinely could (e.g. "tire uma foto" and, moments later in a separate turn, "identifique essa planta"), only `processar_funcao_visual` (`analisar_tela`/`analisar_camera`) had a mutex (`self.executando_funcao_visual`) guarding it; `identificar_planta`, `consultar_segunda_opiniao_visual`, `salvar_print_tela`, `tirar_foto_camera`, and `_obter_ou_capturar_ultima_captura` all called `capturar_camera_bytes()` (or, for `salvar_print_tela`, `capturar_monitor_do_cursor_bytes()` — screen, not camera, a pre-existing inaccuracy in an earlier version of this exact bullet) directly with no guard at all, and the two "Analisar" buttons (`solicitar_analise_tela`/`solicitar_analise_camera`) bypassed the mutex too by calling `enviar_tela_para_gemini`/`enviar_camera_para_gemini` directly instead of going through `processar_funcao_visual`. **Fixed**: `_mutex_funcao_visual()` (an `@contextlib.asynccontextmanager` right after `processar_funcao_visual`) exposes the *same* `self.executando_funcao_visual` flag to every other capture site — same fail-fast semantics as `processar_funcao_visual` itself (busy → an immediate "tente de novo" message, never waits for the mutex, so a pending `tool_call` can't be held open indefinitely). Applied at all five gaps above; `salvar_print_tela` was included too, for consistency with the mutex's own stated scope ("controla as capturas de tela e câmera"), even though screen capture alone was never exposed to the device-contention bug. The two buttons were switched to call `processar_funcao_visual("analisar_tela"/"analisar_camera", origem="botão")` instead of calling `enviar_*_para_gemini` directly — reuses the existing cooldown+mutex+status logic wholesale rather than duplicating it, and `origem` (new, default `"voz"`) keeps the status-bar text ("Comando de voz detectado" vs. "Botão pressionado") accurate for whichever path triggered it. **Never** call `_mutex_funcao_visual()` from inside `processar_funcao_visual` itself (or anything it calls) — the mutex would already be held, and re-acquiring it would report "busy" against itself. Verified with a 9-case offline harness (real `GeminiLiveWorker`, mocked capture/save functions and `PACOTES_REGISTRADOS`, no live camera or Gemini connection): the mutex's basic acquire/busy/release cycle; `tirar_foto_camera` and `salvar_print_tela` each captured exactly once when free and refused (zero extra captures) when busy; `identificar_planta` correctly skipped the whole `PACOTES_REGISTRADOS` dispatch loop (not just refused after dispatching) when busy, so a package's `despachar()` never runs without the `imagem_bytes` it needs; `_obter_ou_capturar_ultima_captura` refused when busy and worked normally when free; and both buttons confirmed routing through `processar_funcao_visual` with `origem="botão"`.
     - Verified with a standalone test harness (constructing a real `GeminiLiveWorker`, a fake `sessao`/`tool_call`, and temporarily monkeypatching `PACOTES_REGISTRADOS` with a package whose `despachar()` sleeps for a controlled duration) rather than a live voice call, since the point being tested is exact wall-clock timing/concurrency, which is far more reliably verified this way: two 1.5s calls issued together completed in ~1.5s total (not ~3s), confirming genuine parallelism; a call exceeding a (temporarily shortened) timeout was cancelled and answered with an error `FunctionResponse` in ~1s instead of hanging for its full 3s; an unhandled exception inside a package's `despachar()` was caught and reported instead of crashing or hanging; a finished task was confirmed removed from `self.tarefas_funcao_ativas` afterward; and the pre-existing fast/normal path (an unrecognized function name, hitting the native "Função desconhecida" branch) was confirmed unaffected, returning in ~7ms exactly as before.
     - **Follow-up, same session: two-phase "hybrid" response, so the jarvis can keep talking about anything else while a slow function is still running** — the user asked directly ("não tem como fazer ele responder enquanto executa uma ação?") after learning that, even with the concurrency fix above, the model still can't generate *any* new speech until it receives the `tool_response` for a pending `tool_call` (a real limitation of the Live API's function-calling protocol, not a client bug — confirmed by reasoning through the protocol, not by a failed live test). Solved with a two-phase design in `_executar_chamada_de_funcao_com_timeout`, chosen over the two simpler alternatives (apply to every function, or hardcode it to `admin_terminal` only) specifically because the user picked the hybrid option when offered a choice:
       - **`processar_chamada_de_funcao` no longer sends `sessao.send_tool_response` or schedules `encerrar_apos_resposta` itself** — it now just `return`s `(function_responses, encerrar_depois)` (a real, deliberate signature change; there is exactly one `return` in that method now, confirmed via `grep`). Delivery is entirely the caller's decision, because the caller might need to route the eventual result somewhere other than a normal `tool_response`.
       - **FASE 1**: `_executar_chamada_de_funcao_com_timeout` starts `processar_chamada_de_funcao` as its own `asyncio.Task` immediately, then races `asyncio.wait_for(asyncio.shield(tarefa_execucao), timeout=min(LIMITE_RESPOSTA_IMEDIATA_SEGUNDOS, timeout))` — `LIMITE_RESPOSTA_IMEDIATA_SEGUNDOS` is 5s. `asyncio.shield` is the crux of the whole mechanism: without it, a Phase 1 timeout would cancel `tarefa_execucao` itself (wrong — the work should keep running); shielded, only the *waiting* is abandoned, the task keeps executing for Phase 2 to pick back up. If it finishes within 5s (the overwhelming majority of calls — memory, vision, etc.), it's delivered exactly like before (new helper `_enviar_resposta_funcao`, which is just the old tail of `processar_chamada_de_funcao` moved out unchanged) — **zero behavior change** for anything fast.
       - **FASE 2** (only entered if Phase 1 timed out): sends a provisional `FunctionResponse` per call in the batch — via the *existing* `_responder_falha_para_lote` helper, reused here for a "still working" message rather than a failure — instructing the model to tell the user it already started and to keep talking normally, not wait in silence. This closes that specific function-call's turn from the Live API's perspective. From this point on, the real result (success, failure, or the *outer*, longer per-function timeout from `TIMEOUTS_TAREFA_FUNCAO_POR_NOME`/`TIMEOUT_TAREFA_FUNCAO_SEGUNDOS` firing) can **never** be delivered via `send_tool_response` again — there is no tested, reliable way to answer an already-answered function call on the Live API — so it goes out instead via `_enviar_anuncio_espontaneo` (`self.sessao`, not the `sessao` parameter — deliberate: the announcement should go to whatever session is *currently* live, not a possibly-stale reference captured when the task started), the exact same spontaneous-announcement channel `admin_terminal`'s notification-confirmation path and `rede_jarvis` already use. `verificar_inatividade()`'s existing precedent (call `_enviar_anuncio_espontaneo` directly, not the cross-thread `_falar_espontaneamente` wrapper, since this code already runs on the worker's own loop) is followed here too.
       - **Verified the actual claim, not just the plumbing**: a fourth standalone test (`testar_conversa_durante_execucao`) started a slow call, let it fall into Phase 2 (confirmed via the Phase-1 "andamento" `FunctionResponse`), and then — *while that Phase 2 wait was still in progress in the background* — issued a second, unrelated call through the same `_executar_chamada_de_funcao_com_timeout` path and confirmed it returned in ~1ms, not blocked by the first at all. Also verified: the fast path is provably unchanged (single `FunctionResponse`, zero spontaneous announcements); the successful-after-Phase-2 path sends exactly one provisional response *and* exactly one spontaneous announcement carrying the real result (never two `tool_response`s for the same call); and Phase 2 timing out still cancels the task and announces a cancellation, matching Step 3 of the original concurrency task.
   - **Live session stall detection and auto-recovery, replacing a real "whole app hangs, has to be force-killed" bug** — the user asked jarvis to send a screenshot to a Discord channel (fixed above), it did it wrong, and when told so it said it would retry and then went completely silent — no reply, nothing sent, the app itself became unresponsive and had to be killed via Task Manager. This is a different class of bug than the earlier concurrency/timeout work: that protected individual *function calls* from hanging; this is the underlying Gemini Live WebSocket *connection itself* stalling silently (no exception, no clean close) — and before this fix, **nothing in the codebase had any timeout on anything sent to the session** (`send_client_content` or `send_realtime_input`, across all 10 call sites in the file — the mic loop in `enviar_microfone` included, the highest-frequency one), so a stalled connection meant an `await` that would simply never return, forever. The user's own suggestion (fall back to Groq when Gemini "freezes") doesn't actually solve this: Groq has no TTS/voice integration anywhere in this project (`delegacao_ia` uses it purely for text completions), so there's no path for it to produce spoken output through jarvis even if Gemini's connection were swapped out — flagged directly to the user rather than building something that couldn't deliver what was asked. Fixed instead with:
     - **`_enviar_para_sessao(corrotina)`** — a single wrapper every one of those 10 send call sites now routes through (`await self._enviar_para_sessao(self.sessao.send_client_content(...))` etc., passing the not-yet-awaited coroutine in). Wraps in `asyncio.wait_for(..., timeout=TIMEOUT_ENVIO_SESSAO_SEGUNDOS)` (10s); on a timeout *or any other exception*, sets `self.conexao_travada = True` and re-raises unchanged — existing callers that already have their own `except Exception` (like `enviar_tela_para_gemini`/`enviar_camera_para_gemini`) keep working exactly as before, this only adds the timeout and the stall flag on top.
     - **`monitorar_conexao()`** — a fifth concurrent task in `executar()` (alongside mic/receive/playback/inactivity), polling `self.conexao_travada` every 1s. The moment it's set, it flips `self.ativo = False` and returns — **deliberately does not attempt a spontaneous voice announcement first** (unlike `verificar_inatividade`'s inactivity auto-hangup), because that announcement is itself a `send_client_content` call routed through the same `_enviar_para_sessao` — trying to announce a stalled connection could hang on the exact same problem being recovered from. `executar()`'s existing `while self.ativo: ...` loop and cleanup path handle the rest unchanged; no new shutdown mechanism was added.
     - **Verified with a standalone test** (a fake session whose `send_client_content` never returns): confirmed `_enviar_anuncio_espontaneo` raised `TimeoutError` in ~1s (temporarily shortened `TIMEOUT_ENVIO_SESSAO_SEGUNDOS`), not left hanging; confirmed `conexao_travada` flips to `True` on that path and stays `False` on a normal successful send; and confirmed `monitorar_conexao` independently detects the flag and flips `self.ativo` to `False` within its ~1s poll interval. Full concurrency/timeout/hybrid-response and Discord-channel-capture regression suites re-run clean after this change too.
     - **A second, related bug found and fixed in the same pass, from the user's very next test run**: `enviar_microfone`'s `with sd.RawInputStream(...):` had **zero error handling** — the exact `sd.RawInputStream` construction that `jarvis/pacotes/ativacao_voz/detector.py` already wraps in a `try`/`except` (by design, documented as required graceful degradation) was, for the *real call's own mic*, completely unguarded. When opening the mic failed (the user hit this literally — console showed `[ativacao_voz] Não foi possível abrir o microfone: Error querying device -1`, meaning no default input device was resolvable by PortAudio at that moment), `enviar_microfone`'s `asyncio.Task` died silently with no done-callback tracking it (unlike `self.tarefas_funcao_ativas`) — the call stayed nominally "connected" (beep already played, status showed normal) but the user's audio was never captured again, matching the reported "não responde mais" exactly. Fixed by wrapping the stream-open-and-listen loop in `try`/`except Exception`: on any failure (opening the stream, or anything inside the loop, `_enviar_para_sessao` failures included), it now prints a clear `[MICROFONE]` message, emits `self.erro_recebido` so the UI actually shows something, and sets `self.ativo = False` to end the call cleanly instead of leaving it in a zombie "looks connected, silently deaf" state. Verified with a standalone test reproducing the *exact* error string the user saw (`OSError("Error querying device -1")`, via `unittest.mock.patch` on `sd.RawInputStream`): confirmed `self.ativo` flips to `False` and exactly one `erro_recebido` signal fires, mentioning "microfone".
   - **Silently-dying call tasks and the un-timed-out `send_tool_response` — a third, distinct "app froze, had to restart" bug, reported after the folder restructure** — the user reported the call freezing with the status label stuck at `Email preparado, aguardando confirmação...` and, on an earlier occasion, at `Consultando memórias...`, needing an app restart both times. **Not caused by the restructure**: the entire code diff of that reorganization was imports, path constants, and `garantir_pasta()` — nothing touching asyncio, threading, or the session (verified by filtering the diff down to non-comment lines). The freeze had two independent, pre-existing root causes, both in `jarvis/gemini/cliente_live.py`, and both matching the *exact* pattern already fixed once for `enviar_microfone`:
     - **Cause 1 — the five concurrent call tasks were unsupervised.** `executar()` creates `enviar_microfone`/`receber_audio`/`reproduzir_audio`/`verificar_inatividade`/`monitorar_conexao` with `asyncio.create_task(...)` and then sits in `while self.ativo: await asyncio.sleep(0.1)`; the tasks are only ever awaited by the `asyncio.gather(*tarefas, return_exceptions=True)` that runs *after* `self.ativo` is already `False`. So an exception in any of them was observed by nobody: the task died, `self.ativo` stayed `True`, the keep-alive loop spun forever, and the exception was finally swallowed by `return_exceptions=True` when the call eventually ended. `receber_audio` was the severe case — it had **no `try`/`except` at all** over its `async for resposta in sessao.receive():` loop, so any error there (dropped connection, protocol error) meant no Gemini response, audio or `tool_call`, was ever processed again, while the mic task kept happily streaming and the UI kept showing the last status emitted. That is precisely "status parado, tive que reiniciar", and it explains why the freeze landed on two unrelated tools: the freeze is not in the tool, it is in what happens after it. Fixed with `_tarefa_supervisionada(nome, corrotina)`, one small method wrapping each of the five `create_task` calls — on any `Exception` it prints, emits `self.erro_recebido` (so the UI shows something) and sets `self.ativo = False`, ending the call cleanly instead of leaving a zombie. `asyncio.CancelledError` is deliberately *not* caught (it is not an `Exception` subclass since 3.8), so the normal cancellation `executar()` performs on shutdown still passes straight through and is never reported as a failure. `enviar_microfone` keeps its own `try`/`except` on top of this, because its message is microphone-specific.
     - **Cause 2 — `send_tool_response` was the only session send in the file with no timeout.** `_enviar_resposta_funcao` and `_responder_falha_para_lote` both did a bare `await sessao.send_tool_response(...)`, bypassing `_enviar_para_sessao` (all 10 `send_client_content`/`send_realtime_input` sites already went through it). Since the Live API's protocol means the model literally cannot speak again until it receives the `tool_response`, a stall there froze the call permanently with nothing able to recover it — `conexao_travada` was never set, so `monitorar_conexao` never fired. Both now route through `_enviar_para_sessao`.
     - **The Phase-1 `try` had to be restructured for Cause 2's fix to be safe**, and this is the subtle part: `_enviar_resposta_funcao` used to be called *inside* the same `try` whose `except asyncio.TimeoutError` means "the function is still running, send the provisional 'em andamento' response and go to FASE 2". Once the send itself can raise `TimeoutError`, leaving it in that `try` would make a failed send look like a slow function — sending a provisional `FunctionResponse` **and then** a FASE 2 spontaneous announcement, i.e. answering the same `chamada.id` twice, which the Live API does not support (see the two-phase bullet above). The send now lives in an `else:` clause on that `try`, so its exception propagates instead: `_enviar_para_sessao` has already set `conexao_travada`, and `monitorar_conexao` ends the call.
     - **Also fixed in the same pass (same bug class, not the cause):** `salvar_memoria`/`listar_memorias`/`esquecer_memoria` were called **directly on the event loop** in `processar_chamada_de_funcao`, not via `asyncio.to_thread` — file I/O under a `threading.Lock`, violating this file's own standing rule (the same rule whose violation at the camera-capture call sites caused the "responds, then goes deaf" bug documented above). Ruled *out* as the freeze cause (only the worker thread ever touches `memory`, so there is no lock contention, and `dados/memoria.json` is tiny), but wrapped anyway. `contexto_memorias()` at session-setup time is deliberately left as a direct call — it runs before the concurrent tasks exist, so there is no loop to stall.
     - **Verified with a 6-case offline harness** (real `GeminiLiveWorker` with the MQTT/Discord/admin starters monkeypatched out, a fake `sessao` whose `send_tool_response` sleeps 3600s to simulate a stalled connection): a raising `receber_audio` now flips `self.ativo` to `False` and emits exactly one `erro_recebido` instead of hanging; a stalled `send_tool_response` gives up in ~1s (temporarily shortened `TIMEOUT_ENVIO_SESSAO_SEGUNDOS`) and sets `conexao_travada` instead of blocking forever; `monitorar_conexao` then ends the call; the fast path is provably unchanged (exactly one `send_tool_response`, zero spontaneous announcements); a send failure produces **zero** responses and **zero** announcements rather than a double answer; and a normal task cancellation is not reported as a failure. The app was also launched for real afterwards and ran clean.

   - **Stall watchdog (`vigiar_travamento`) plus two more silent-stall gaps, from a third freeze report — this one during a fullscreen game** — the user asked for `analisar_tela` while gaming; jarvis **did answer** (described the screen correctly) and only then went silent, status frozen at `Tela enviada para análise.`. That "answered, then died" shape rules out the tool itself and the `tool_response` (both completed), and points at the audio/receive plumbing afterwards. Two concrete gaps were found, both able to stall **without raising anything**, so the `_tarefa_supervisionada` net added in the previous pass could not catch them:
     - **`reproduzir_audio`'s `await asyncio.to_thread(saida.write, audio_bytes)` had no timeout.** One block is `BLOCO/TAXA_SAIDA` ≈ 43ms. If the output device stops consuming — a fullscreen game taking the sound card in exclusive mode is the obvious real-world trigger — that write just blocks, forever, with no exception. Playback dies mute while everything else keeps working: the model keeps answering, `fila_saida` (which is `asyncio.Queue()`, **unbounded** — so there is no backpressure deadlock, just unbounded growth) keeps filling, and the user hears nothing and sees the last status forever. Now wrapped in `asyncio.wait_for(..., timeout=TIMEOUT_ESCRITA_AUDIO_SEGUNDOS)` (10s, ~230× a normal block); the resulting `TimeoutError` propagates to the supervisor, which ends the call with a visible error instead of a mute zombie. (`_tarefa_supervisionada` also had to learn that `str(TimeoutError())` is empty — it now falls back to the exception's class name, or the UI message would have ended in a bare colon in exactly this case.)
     - **`receber_audio`'s `while self.ativo: async for resposta in sessao.receive():` could hot-spin.** If `receive()` returns *without yielding anything* (server-side session over), the outer `while` immediately calls it again — a busy loop burning CPU while never processing another response, again with zero exceptions, and especially damaging on a machine already loaded by a game. A `recebeu_algo` flag now ends the call cleanly in that case, with a clear message, instead of spinning.
     - **`vigiar_travamento(fila_saida)` — a sixth supervised task, the real point of this pass.** Three freeze reports had produced *zero* diagnostic output, so the app now watches its own invariants every `INTERVALO_VIGILANCIA_SEGUNDOS` (5s) and reports, to the console **and** to the UI via `erro_recebido` (`_reportar_travamento`), which one broke. It deliberately checks only states that are *impossible* in normal operation, never "nothing is happening" (idle is the normal case, so idleness would be a false-alarm generator): `alfred_falando` continuously `True` past `LIMITE_FALANDO_PRESO_SEGUNDOS` (60s — `liberar_microfone_apos_fala` clears it 0.8s after the last block, so a minute means that task was lost, which in default non-interruption mode means a permanently mute mic); `executando_funcao_visual` stuck past `LIMITE_FUNCAO_VISUAL_PRESA_SEGUNDOS` (120s — its `finally` should make this impossible, but stuck it blocks every screen/camera analysis for the rest of the call); `fila_saida` above `LIMITE_FILA_SAIDA_PARADA` (200) and not draining while nothing has been written to the device (playback stalled); and any task in `self.tarefas_chamada` that is `done()` and not `cancelled()` while the call is still active. Where it is safe, it **un-sticks** the state (clears `alfred_falando`/`executando_funcao_visual`) rather than only complaining, so the call recovers instead of forcing an app restart. All six call tasks now get a `name=` on `create_task` purely so this watchdog can say *which* one died. The playback-stall warning is fired once per episode (`ja_avisou_reproducao`), not every 5s, so it can't flood the activity log.
     - **Verified with a 6-case harness**: a hung output device gives up in ~1s (temporarily shortened timeout) and reports `REPRODUÇÃO ... TimeoutError` instead of blocking; an empty `receive()` ends the call after exactly **one** call to `receive()` instead of spinning; the watchdog detects and clears both a stuck `alfred_falando` and a stuck visual mutex; it reports a stalled playback queue exactly once; and — the case that matters most for a watchdog — it raises **zero** false alarms across a simulated sequence of normal short speech turns with playback progressing. The previous pass's 6 tests still pass, and the app was launched for real afterwards (14s, clean).
     - **The echo/interruption theory was raised and then RULED OUT by the user — do not resurrect it**: the user games on a **headset**, so no speaker audio has ever leaked into the microphone. `config.json` does have `"interrupcao": true` on this machine, and the mechanism below is real in principle, but it is not what is happening here. Kept only so nobody re-derives and re-proposes it: `"interrupcao": true` In that mode the mic is never gated by `alfred_falando` (all three checks in `enviar_microfone` carry `and not self.interrupcao_habilitada`), so **game audio coming out of speakers goes straight back into the microphone**. The server's VAD then reads that as the user talking and raises `server_content.interrupted`, which `receber_audio` handles by flushing `fila_saida` and clearing `alfred_falando` — i.e. every answer jarvis starts gets cut off immediately, indistinguishable from "it stopped responding". This is the "no-headphones echo risk" already flagged in the interruption section below, and no amount of code hardening fixes it: it is acoustic. If freezes keep happening specifically while gaming on speakers, test `"interrupcao": false` before hunting further in the code.

   - **Dead-microphone detection — the leading explanation for the freeze that survived every earlier fix** — decisive detail from the user: the fullscreen-game freeze happened **after restarting with the supervisor already in place**, and **no error appeared in the UI**. Since `_tarefa_supervisionada` emits `erro_recebido` on any `Exception`, and `_enviar_para_sessao` flags `conexao_travada` on any send timeout, that combination rules out both an exception *and* a send stall: whatever breaks does so without raising anything at all. The remaining candidate that fits every fact is the **input stream going silent**. `enviar_microfone` opens one `sd.RawInputStream` at call start and then blocks on `await fila_microfone.get()` forever; if the default input device changes or re-enumerates mid-call — a game grabbing the audio endpoint, a USB headset re-initialising — PortAudio simply **stops calling the callback**. No exception, the stream object still looks open, the queue never receives another block, and jarvis never hears the user again while the call still looks perfectly alive with the last status frozen on screen. Restarting the app "fixes" it because the new call opens the stream on whatever device is current. Detection: the callback now stamps `self.timestamp_ultimo_bloco_microfone = time.monotonic()` as its **first** statement, deliberately before every early return (including the `alfred_falando` drop in default mode), because the signal being measured is "the device is still delivering blocks", not "the audio is being used" — putting it after those returns would make a long spoken answer look identical to a dead microphone. `vigiar_travamento` reports and ends the call when that stamp is older than `LIMITE_MICROFONE_MUDO_SEGUNDOS` (10s); a `0.0` stamp means the stream has not opened yet and is ignored, so startup never false-alarms. Ending the call rather than trying to reopen the stream in place is deliberate: the next call opens a fresh stream on the current device, which is the actual recovery, and it does not require restarting the app. **Threshold validated against the real microphone on this machine, not assumed**: 3s of real capture with the app's own parameters (`TAXA_ENTRADA=16000`, `BLOCO=1024`) gave 46 blocks, 64ms mean interval, **71ms worst case** — the 10s limit is ~140× the worst observed gap, so a healthy stream cannot trip it. Also covered by three offline tests: a stale stamp ends the call with the right message; a stamp refreshed every 64ms raises nothing even while `alfred_falando` stays `True` throughout (the long-answer false-positive case); and an unopened stream raises nothing.

   - `cerebro_reserva/` — **REMOVED from the project.** It used to take over the voice conversation when the Gemini Live session failed (Groq Whisper to hear, Mistral to think, edge-tts/SAPI to speak), and also to assume temporarily mid-call when Gemini went silent (`vigiar_resposta_lenta` → `_conduzir_reserva_temporaria`, the "hot standby"). Removed at the user's explicit request after its cost was measured: Mistral's `chat/completions` is stateless, so every single request re-sent the full 45-tool schema — **7,480 tokens measured** (a bare "bom dia" cost 7,511 prompt tokens instead of 31), which against the 25,000 tokens/min limit of `mistral-medium-latest` allowed roughly **1.5 tool-using turns per minute**.
     - **What came out of `jarvis/gemini/cliente_live.py` with it** (~540 lines): `vigiar_resposta_lenta` (the sixth call task), `_conduzir_reserva_temporaria`, `_devolver_controle_ao_gemini`, `_anunciar_retomada_gemini`, `_status_reserva`, `_ao_finalizar_tarefa_reserva`, `self._buffer_circular_mic` and its feeding, the `_fila_bloco_reserva` mirror in the mic callback, `LIMIAR_ATIVIDADE_VOZ_USUARIO`/`timestamp_ultima_atividade_voz_usuario`, and the flags `reserva_ativa`/`reserva_falando`/`suprimir_audio_pos_reserva`. Also: the `configuracoes` section, the two prompts (`CEREBRO_RESERVA_INSTRUCAO_SISTEMA`, `RETOMADA_CEREBRO_RESERVA`), and the `edge-tts`/`mutagen` dependencies (confirmed used by nothing else — `GROQ_API_KEY`/`CEREBRAS_API_KEY` stayed, `delegacao_ia` uses them; `MISTRAL_API_KEY` stayed, `identificacao_visual` uses it).
     - **What replaced it: nothing takes over the conversation.** A call that dies from a failure now says so in the console and in the UI (`erro_recebido`) at the end of `executar()`, and the user starts another call. The documented "Gemini goes completely silent for 15-30s mid-call" problem therefore has **no mitigation any more** — this was stated to the user before removing, and accepted; the intended substitute is switching `PROVEDOR_IA` to `openai`, which is why that provider was added in the first place.
     - **What deliberately survived, and why** — worth knowing before assuming any of it is also dead: `self.transcricao_conversa` (the reserve read it, but it is also what becomes the conversation summary saved to memory at the end of a call, and what carries across an automatic reconnection), `input_audio_transcription` on the `LiveConnectConfig` (same reason — it is what fills the user side of that transcript), `encerrou_por_falha` (now only used to report the failure), and the three microphone guards in `enviar_microfone`, which reverted from `(self.alfred_falando or self.reserva_falando) and not self.interrupcao_habilitada` to `self.alfred_falando and not self.interrupcao_habilitada` — the interruption exception is untouched. The transcript pruning limit, which came from `cerebro_reserva.config.MAXIMO_MENSAGENS_HISTORICO`, became the constant `MAXIMO_MENSAGENS_TRANSCRICAO` (12) in each of the two clients.
     - **Verified with 45 dedicated checks** on top of the existing suites (314 total, all passing): no non-comment line in `cliente_live.py` mentions any of the 15 removed symbols; the package folder is gone; `GeminiLiveWorker` no longer has the four removed attributes while the six that had to survive still exist; `enviar_microfone` keeps exactly three `alfred_falando` guards each with its `not self.interrupcao_habilitada` exception; `_laco_reproducao` no longer waits on a voice that cannot speak; `executar()` creates the six remaining tasks and not `VIGIA_RESPOSTA`; and the settings screen no longer offers any `RESERVA_*` variable. The app was also launched for real afterwards.

   - **Dedicated audio executor, from a "freezes while speaking but the PC is idle" report** — the user reported the call freezing *mid-speech* while insisting (correctly) that the machine was not short of resources: Task Manager showed low CPU, RAM, disk and GPU throughout, and they asked whether the app could be made to use *more* of them. **Low resource usage during a freeze is evidence against a resource shortage, not for one**: a process that is frozen while consuming no CPU is *waiting on something*, not waiting *its turn*, and more RAM or GPU cannot shorten a wait. The real defect was in scheduling, and it is the honest translation of "run with more headroom":
     - **`reproduzir_audio` wrote every audio block through `asyncio.to_thread`, i.e. the loop's shared default executor — the same pool used by all 36 other blocking call sites in this file** (tool dispatch, camera captures, admin commands, email). That pool holds `min(32, cpu_count + 4)` workers; when it fills, an audio block simply *queues*. Measured on this machine (24 logical cores → 28 workers): with the pool busy, one block waited **1,69s**, about **40 blocks** of speech, i.e. the sentence stopping dead — while the CPU reads as idle the whole time, because those workers are blocked on I/O rather than computing. That is exactly the reported symptom.
     - Fixed by giving playback its **own** `concurrent.futures.ThreadPoolExecutor(max_workers=1)`, created per call inside `reproduzir_audio` and used via `laco.run_in_executor(...)` instead of `asyncio.to_thread`. The body of the loop moved unchanged into `_laco_reproducao(...)` so the executor could be created and torn down around it; the teardown is `shutdown(wait=False, cancel_futures=True)` so ending a call never waits on a write stuck in the device. **Measured improvement: 2,59s → 0,0005s for a block dispatched while the shared pool is saturated (≈5600×).**
     - `LATENCIA_SAIDA = "high"` was made explicit on the output stream. It was already the effective default, but it is now a visible knob: measured, `"low"` gives 128ms of buffer (3 blocks of slack) and `"high"` gives 213ms (5 blocks). Raising it costs no measurable CPU or memory — it trades milliseconds at the start of speech for tolerance to system hiccups.
     - **Interruptions are now logged** (`self.interrupcoes_na_chamada` + an `[INTERRUPÇÃO]` line naming the count and how many queued blocks were discarded). This targets the other candidate for the same symptom, which no amount of code can fix: with `"interrupcao": true` and a boom-mic headset, a keyboard click, a breath, or earcup bleed reaching the mic makes the server raise `interrupted`, and `receber_audio` responds by flushing `fila_saida` — the sentence stops mid-word and never resumes, consuming no CPU. **If that counter climbs while the assistant speaks and the user is silent, the fix is `config.json` → `"interrupcao": false`, not a code change.**
     - **Optional process priority**, opt-in via `config.json` → `"prioridade_alta": true` (`preferencias.prioridade_alta_ativa()` / `aplicar_prioridade()`, called once from `main()`). Uses `psutil` (already a dependency) and deliberately sets `ABOVE_NORMAL_PRIORITY_CLASS`, never `HIGH`/`REALTIME`, which could starve the rest of the system — including the game — and trade one problem for a worse one. It does **not** make the app consume more of anything; it only changes queue order under contention, which is the one legitimate sense in which "give it more CPU" applies here. Never raises and never blocks startup: a missing `psutil` or a refusal from Windows just prints a warning and continues at normal priority.
     - Verified with three tests: a saturated shared pool delaying a dispatch by 2,59s while the dedicated executor answers in 0,5ms; the **real** `reproduzir_audio` loop writing 10 blocks (0,43s of audio) to the actual device in 0,46s, i.e. correct real-time pacing; and the executor teardown returning in 0,000s with a write still stuck. The priority helper was exercised both off (returns `False`, changes nothing) and on (process class actually changed, then restored).

   - **Console panel beside the activity log (`jarvis/ui/painel_console.py`)** — requested by the user after three rounds of freezes whose only diagnostic output went to a terminal nobody watches. The activity log shows what the assistant is *doing* (high-level status); this panel shows what is *happening underneath*: interruptions, watchdog warnings, connection failures, tracebacks. It **duplicates `sys.stdout`/`sys.stderr`** rather than being wired into each call site, so every existing `print` — `[INTERRUPÇÃO]`, `[VIGIA]`, `[CONEXÃO]`, `[MICROFONE]`, `[RESERVA]`, `[PRIORIDADE]`, plus anything a package prints and any traceback — shows up with no further changes. The original streams are never replaced, only echoed, so the terminal keeps working; `restaurar_saida_padrao()` runs in `closeEvent` so a late print can't write into a destroyed widget. Prints arrive from many threads (worker, MQTT listener, voice detector, package threads) and Qt widgets are GUI-thread-only, so writes cross through a `Signal` on an internal `QObject` with an explicit `QueuedConnection` — the same principle as `jarvis/nucleo/sinalizador.py`. Partial writes are buffered until a newline (a single `print` calls `write()` more than once), lines are timestamped and colour-coded by severity, and `document().setMaximumBlockCount(600)` caps growth so a diagnostic panel can never become the cause of the next freeze. Errors that reach the UI only as a `erro_recebido` Signal (never printed) are pushed in explicitly from `mostrar_erro`. The main window's minimum size went 560→900 wide, since two columns at 560px would leave both unreadable.
     - **Found while doing this: 13 of the 16 rules in `ESTILO_GLOBAL` are dead, and always have been.** The stylesheet comments its rules with `#`, but QSS has no `#` comment — only CSS-style block comments. A line starting with `#` parses as an id selector, and the real selector on the next line becomes its *descendant*, so the rule can never match. Proven empirically: the same `QTextEdit#console` rule applies when used alone and does **not** apply inside `ESTILO_GLOBAL`, and converting every `#` comment to a block comment makes all of them work. Only the 3 rules that happen to follow a blank line (e.g. `QPushButton#botaoChamada:hover`) were ever live — which is why the window renders in the system dark theme instead of the white/red design the code describes. **Not fixed here on purpose**: repairing it restyles the entire app to a light theme the user never asked for. The one-line fix is `re.sub(r"(?m)^#(.*)$", r"/* */", ESTILO_GLOBAL)`; it is the user's call. The console panel therefore styles **itself**, via `setStyleSheet` on its own widgets in `painel_console.py`, which is both better encapsulation and the only way its styling could work at all while the global sheet stays broken.
     - Also beware when writing QSS block comments: a `*/` inside the comment text closes it early and breaks the following rule. Hit exactly that while writing the comment explaining this very bug.
     - **Verified with 9 tests against a real `MainWindow`**: the panel exists with its opening line; a `print` from the main thread appears; a `print` from **another thread** arrives safely on the GUI; a `traceback.print_exc()` on stderr appears; a worker error delivered by Signal (never printed) appears; `sys.stdout` is confirmed *duplicated* rather than hijacked; 1200 lines of flood stay capped at the 600-line ceiling; the Limpar button empties it; and `restaurar_saida_padrao()` gives the real stdout back. The layout was also checked visually with a screenshot of the rendered window.

   - `memoria_obsidian/` — **replaced `memory/memory_manager.py` as the live memory system.** Memory is now a folder of `.md` notes linked with `[[wikilinks]]` (an Obsidian vault), instead of ≤50 short sentences in a JSON file. The Obsidian app is never required — the package only reads and writes plain text files; opening the folder in Obsidian later is optional and the links already work. The old `jarvis/servicos/memoria/gerenciador.py` and `dados/memoria.json` are **kept, untouched and out of the flow**, as the migration source and as a backup of what existed before.
     - **The real change is not the format, it's how memory reaches the model.** Before: every memory was pre-loaded into `instrucao_sistema` at session start. Now: only the `NOTAS_CONTEXTO_INICIAL` (5) most recently used notes go in, and the model pulls the rest on demand via `buscar_memorias_relacionadas`. That is what lets memory grow without inflating the prompt every session.
     - **It is a normal tools package**, so `cliente_live.py` got *smaller*: the three native memory `FunctionDeclaration`s and their three dispatch branches were deleted, replaced by one import + one `PACOTES_REGISTRADOS` entry. `cerebro_reserva` (since removed) inherited the memory tools with **zero** extra wiring, which was the package contract paying off.
     - **Note lifecycle, and no stage is ever skipped**: `ativa → arquivada → resumida (only then is the original deleted)`. A note is archived only when **all three** criteria hold: `last_used` older than 90 days **AND** `access_count < 2` **AND** `pinned == false`. Archiving **moves** to `arquivo/`, never deletes. An original is removed only after it has been included in a summary that was **successfully written to disk** — verified live twice by accident: a 404 (retired model) and a 503 (overloaded model) during testing both left all 16 notes intact.
     - `pinned: true` is absolute — set by voice ("isso é importante, não esqueça nunca" → `fixar=true`), never pruned. Summaries are written pinned too, so a summary is never re-summarized.
     - **Mass-delete refusal was carried over deliberately**, not reimplemented loosely: `esquecer.COMANDOS_APAGAR_TUDO` keeps the old set and adds spoken variants. Ambiguous titles return the candidate list instead of deleting a guess, matching the convention used across the project.
     - **Path safety**: a note title is user/model-controlled input, so `notas.nome_arquivo_do_titulo()` strips path and forbidden characters, and `notas.caminho_seguro()` resolves and *raises* unless the result is inside `PASTA_VAULT_JARVIS`. Same reasoning as the email-attachment sanitization in `jarvis/servicos/email/leitor.py`. Tested with `../../` traversal.
     - **Auto-linking had to be redesigned after measuring it.** The spec was "link when the content mentions an existing note's title", implemented first as a plain title-in-content substring — and against the **real migrated notes** it fired **zero** times, because the old system stored bare sentences, so migrated titles are whole sentences ("Email do Gabriel gabrielrapolinario0@gmail.com") that never appear verbatim in new text. `escritor._trechos_reconheciveis()` now matches contiguous word-runs of the title (2+ words, 10+ chars, containing a ≥4-char non-stopword) or a single ≥6-char non-stopword word. Deliberate bias: over-linking beats under-linking, because a wrong link is visible in the note and easy to delete, while a missing link silently costs the assistant context it actually had. Verified with 5 should-link and 5 should-not-link phrasings — all correct.
     - **`contexto_inicial()` deliberately does NOT count as an access.** Auto-loading a note at session start is not the user having used it; counting it would keep `last_used` permanently fresh and the pruning criteria would never fire on anything.
     - **`MODELO_CONSOLIDACAO` was confirmed live, not remembered**: the first value written (`gemini-2.5-flash`) returned 404 "no longer available to new users, use models/gemini-3.6-flash". `gemini-3.6-flash` takes ~9s vs ~1s for `gemini-3.5-flash-lite`, which is irrelevant here (background thread, nobody waiting) and worth it, since originals are discarded after the summary. A transient 503 then showed a second gap, fixed with 3 attempts and growing backoff. If it 404s again, list the models from the API rather than guessing a name.
     - **Verified with 20 live tests**: 15 covering write/dedupe/auto-link/search/linked-notes/access counting/mass-delete refusal/ambiguity/path traversal/the three prune criteria/pinned immunity/archive-is-a-move/reactivation/summary-failure-deletes-nothing (in a throwaway vault, since these delete real files), plus 5 end-to-end consolidation tests against the **real** Gemini API: 16 archived notes summarized into `resumo-2026-Q3.md`, originals deleted only afterwards, and the summary correctly kept the durable facts (allergy, brother's name, football team, favourite restaurant) while dropping the ephemeral ones (a cancelled meeting, an old wifi password, a sold car's plate). The migration itself was run with the user watching: 6 notes created, original dates preserved, `dados/memoria.json` untouched.

   - **Microphone/speaker selects on the main window (`jarvis/ui/painel_dispositivos.py`)** — two `QComboBox` above the activity log, one per direction. Logic lives in its own module; `janela_principal.py` only imports, instantiates and positions it, same approach as the console panel.
     - **The raw device list is unusable as-is**: Windows exposes the same physical device once per host API, and on this machine `sd.query_devices()` returns **46 entries** for about eight real devices. The panel groups duplicates and shows one row each — measured result here: 46 → **4 microphones and 4 speakers**. Grouping is by name prefix, because MME truncates names at 31 chars ("Microfone (HyperX Cloud Flight") while DirectSound gives the full one ("Microfone (HyperX Cloud Flight for PS)"); the longest variant is what gets displayed. `Windows WDM-KS` is hidden entirely (it re-lists everything with worse names — one real entry here was a driver path containing a newline, which would have broken the combo), as are the "Mapeador de som"/"Driver de som primário" pseudo-devices, since the explicit "Padrão do Windows" option already means exactly that. If filtering ever leaves the list empty, the unfiltered list is shown instead rather than an empty select.
     - **The index used comes from whichever host API PortAudio already treats as default on this machine** (MME here). The app always used that API implicitly, and audio is the fragile part of this project — switching the host API underneath as a side effect of adding a select would be changing two things at once.
     - **What is stored in `config.json` is the device NAME, never the index.** Indices shift whenever any device is plugged or unplugged, so storing one would silently point at the wrong microphone. On startup the name is resolved back to an index; a device that is no longer present falls back to the Windows default with a console warning instead of raising.
     - **Applied through `sd.default.device`**, which means every stream in the project picks it up with **zero changes**: the call's microphone, playback and the wake-word detector. `main.py` applies it before `ativacao_voz.iniciar()`, otherwise the first session would still open the old device.
     - Changing the microphone restarts the wake-word detector (`pausar()`/`retomar()`), since it holds the mic between calls and would otherwise keep the old device until the app restarted. A change during an active call only takes effect on the next one — an already-open stream cannot be repointed.
     - `preferencias.salvar_preferencia()` writes one key while preserving the rest of `config.json`, using the atomic `.tmp`-then-`replace` technique — a corrupted `config.json` would break app startup.
     - **Verified with 10 tests** against the real audio hardware: the list collapses correctly and contains no blank or newline-bearing names; both selects exist with "Padrão do Windows" first; choosing a device saves it and **preserves the other preferences** (`interrupcao` survived); `sd.default.device` receives the right pair; **real input and output streams actually open on the chosen devices**; the choice survives a simulated restart; a device name that no longer exists falls back to the default without raising; and returning to "Padrão do Windows" clears it. The user's real `config.json` was backed up and restored around the run.

   - **Speech interruption, opt-in via `config.json` (not `.env`)** — `jarvis/nucleo/preferencias.py`'s `interrupcao_ativa()` reads `config.json` (project root, gitignored — a machine-local preference, format `{"config": [{"interrupcao": false}]}`), returning `False` (with a console warning, never a crash) for a missing file, missing field, or invalid JSON. Read once into `GeminiLiveWorker.interrupcao_habilitada` at `__init__` — not re-read mid-call; changing the file requires restarting the call/app. This is **not a tools package** (no `obter_function_declarations()`/`despachar()`, not in `PACOTES_REGISTRADOS`) — it's a core change to the mic/audio loop itself, which is why it's documented as its own standalone section in docs/INTEGRATION.md ("Interrupção de fala (config.json)") rather than folded into the packages checklist: the loop's structure is expected to differ in the finished course project, so the section documents the *concept* (which of the three `alfred_falando` checks in `enviar_microfone` to find and how to adapt them, the exact `resposta.server_content.interrupted` field confirmed against the installed `google-genai` SDK, the fila_saida-flush + skip-the-normal-delay handling on interruption, the no-headphones echo risk) rather than line numbers. Default (`false`, including "no config.json at all") is byte-for-byte the pre-existing behavior — all three checks degrade to their original `if self.alfred_falando:` form.

   - `jarvis/pacotes/fechar_app/` — closes an already-running app by spoken name (`fechar_app(nome_falado)`). Same isolated-package contract as the others (`obter_function_declarations()`/`despachar()` — see docs/INTEGRATION.md). Name resolution (`processos.py`) copies the project's usual name-matching technique (accent/case fold, substring, `difflib` cutoff 0.72) rather than importing it, matching this project's per-package duplication convention.
     - **Graceful before forced, always.** `fechador.py` first sends `WM_CLOSE` (via `win32gui.PostMessage`, never `SendMessage`, so a slow/unresponsive app can't block the caller) to every visible, titled top-level window belonging to the resolved process's PID(s) — found via `win32gui.EnumWindows` + `win32process.GetWindowThreadProcessId`. It waits up to `TIMEOUT_FECHAMENTO_GRACIOSO_SEGUNDOS` (5s) for the process to actually exit, giving the app itself a chance to show its own "save before closing?" prompt. Only a process with no visible window at all, or one that doesn't exit within that window, gets `terminate()` and then `kill()` (both effectively `TerminateProcess` on Windows — there's no separate SIGKILL to distinguish, so both are called as belt-and-suspenders exactly as requested).
     - **Protected-process list is a hard block, checked before touching anything.** `PROCESSOS_PROTEGIDOS` (`explorer.exe`, `winlogon.exe`, `csrss.exe`, `services.exe`, `svchost.exe`) is checked by exact, case-insensitive name match — never a prefix/substring match that could be tricked. The ALFRED process itself is protected separately, by **PID** (`os.getpid()`), not by process name — blocking "python.exe"/"pythonw.exe" generically would also refuse to close any unrelated Python process the user happens to be running, which isn't the intent.
     - **Multiple processes sharing the resolved name are all closed together** (e.g. several windows/processes of the same browser), with the final message reporting how many closed gracefully vs. how many were forced — never silently picks just one.
     - Verified live: closing a real running app (graceful path taken, confirmed via the process actually exiting before the forced-close branch ever ran); attempting to close `explorer.exe` (correctly refused, no attempt made at all — not even the graceful WM_CLOSE); closing an app with multiple open windows (all closed, count reported correctly).

   - `jarvis/pacotes/criar_arquivo/` — creates a simple text file by spoken request (`criar_arquivo(nome, conteudo, pasta=None, extensao="txt")`), restricted to an explicit folder allowlist. Same isolated-package contract (see docs/INTEGRATION.md); the one `.env` variable it reads, `PASTAS_PERMITIDAS_CRIACAO` (comma-separated absolute paths, defaulting to this machine's Desktop/Documents/Downloads if unset), is exposed via `config_schema()` and registered in `jarvis/pacotes/configuracoes/pacotes.py`.
     - **`pasta` is never a raw path — always resolved against the allowlist's folder names.** A spoken folder name ("Downloads", "Área de Trabalho") is matched (exact, then substring, accent/case-insensitive) only against the `name` of each `Path` already in `config.pastas_permitidas()`; a name that doesn't resolve is rejected with the actual list of allowed folders, never guessed and never treated as a filesystem path in its own right.
     - **Two independent containment checks, not one** — same defense-in-depth principle as `jarvis/servicos/email/leitor.py::baixar_anexo`: `nome` is sanitized (`os.path.basename` + accent-stripping + a character whitelist, same regex as `leitor.py`'s `_nome_arquivo_seguro`) into a single filename component that can never itself be a path, and then, separately, the fully-resolved destination path is re-checked to actually sit inside the resolved destination folder immediately before writing. Neither check alone is trusted to be sufficient.
     - **Overwrite protection and content limits, both non-silent.** An existing file is never overwritten — it gets a timestamp suffix, same `_caminho_sem_sobrescrever` technique as `jarvis/servicos/email/leitor.py`. Content beyond `LIMITE_CARACTERES_CONTEUDO` (5000 chars — this is for a short voice-dictated note, not a document) is truncated with a note appended to the spoken result, never silently rejected nor silently cut with no explanation — same convention as `chat_jarvis`'s file-drop text truncation.
     - Verified live: creating a file in an allowed folder (succeeded, correct content); requesting a folder outside the allowlist (rejected, with the actual allowed-folder list read back); creating a second file with the same name (first file untouched, second got a timestamp suffix, no overwrite).

   - **`jarvis/pacotes/explorador_windows/` extended to also cover the Desktop, not just Explorer windows** — `obter_arquivo_selecionado()` (`selecao.py`) now checks a foreground Explorer window first (original, unchanged behavior), and only when there is **no** matching Explorer window (a `None` sentinel, deliberately distinct from `(False, mensagem)` — a foreground Explorer window that's merely empty does *not* fall through to the Desktop, since the user clearly meant that specific window) falls back to checking the Windows Desktop itself.
     - **The Desktop isn't visible to `Shell.Application().Windows()`** the way an Explorer window is — it's a `SysListView32` control living inside `Progman`/`SHELLDLL_DefView` (or a sibling `WorkerW` window, depending on Windows version — both are checked), owned by the `explorer.exe` process, not this app's own process. `LVM_GETNEXTITEM`/`LVNI_SELECTED` gives the selected index via an ordinary `SendMessage` (it only returns a number), but reading the selected item's **name** (`LVM_GETITEMTEXTW`) requires a buffer that has to exist inside `explorer.exe`'s own memory space — solved with the classic, documented cross-process technique (`VirtualAllocEx`/`WriteProcessMemory`/`SendMessageW`/`ReadProcessMemory`, the same approach tools like `pywinauto` use to read another process's ListView), implemented in the new `jarvis/pacotes/explorador_windows/desktop.py`. This was confirmed live against a real Desktop selection before being considered viable — not assumed to work from documentation alone.
     - The resolved display name (which may hide its real extension, or be a `.lnk` shortcut) is matched against what actually exists in the real Desktop folder (`SHGetKnownFolderPath`, same technique already used by `captura_tela.py` — never a hardcoded path, since the Desktop can be OneDrive-redirected): exact name first, then name-plus-any-extension; a shortcut is resolved to its real target via `WScript.Shell`'s `CreateShortCut(...).Targetpath` when that target still exists. Never guesses a nonexistent path — a display name that can't be resolved to something real on disk returns a clear failure instead.
     - Verified live: selecting a real file on the Desktop (not inside any Explorer window) and confirming `obter_arquivo_selecionado()` correctly resolves it to its real absolute path.

   - **Packages brought over from `JARVIS COMPLETO` (the finished course project's `actions/` folder)** — nine new isolated packages, all on the standard contract (`obter_function_declarations()`/`despachar()`, see docs/INTEGRATION.md), none needing any session-glue wiring. Each package's `acoes.py` is the course's original file; only what's listed below changed. See docs/INTEGRATION.md, section "Pacotes vindos do JARVIS COMPLETO", for the full table.
     - `jarvis/pacotes/arquivos_area_trabalho/` — 8 tools for Desktop files (`criar_pasta_area_trabalho`, `listar_area_de_trabalho`, `organizar_area_de_trabalho_basico`, `copiar_item_area_trabalho`, `recortar_item_area_trabalho`, `colar_item_area_trabalho`, `renomear_item_area_trabalho`, `cancelar_transferencia_area_trabalho`). `acoes.py` is a literal copy of `actions/file_actions.py`. **The module's central protection is that every path goes through `_esta_dentro_da_area`/`_resolver_caminho_relativo`**, which reject anything outside the Desktop — verified live with `../../..` traversal. Copy/cut are two-step: they only record the item in an in-memory `_AREA_TRANSFERENCIA` (not the real Windows clipboard), and nothing moves until `colar_item_area_trabalho`. **No function in this file deletes anything** (verified: no `unlink`/`rmtree`/`os.remove` anywhere in it), and nothing ever overwrites an existing item.
     - `jarvis/pacotes/abrir_aplicativo/` — **replaced the `abrir_app_local` package, which was deleted** (explicit user decision when offered the choice between merging, coexisting, or replacing). Resolves a spoken name in four steps: fixed Windows aliases (Meu Computador, Explorador, Configurações, Calculadora, Painel de Controle, personal folders), Start Menu `.lnk`/`.url` shortcuts, `Get-StartApps` (Microsoft Store apps), and a fixed dictionary of known executables via `shutil.which`. It has no on-disk cache (the old one had `dados/apps_conhecidos.json`) — the first two steps already answer instantly. **A fifth step was added that did not exist in the course version**: `abrir_de_pastas_extras()` preserves `PASTAS_EXTRAS_APPS` from the deleted package (shallow scan — the folder itself plus one level of subfolders, never a deep `rglob`), because dropping it would silently lose portable programs for anyone who had that variable configured. `executar_comando` still always uses `subprocess.Popen(..., shell=False)`.
     - `jarvis/pacotes/navegador_web/` — **replaced the `navegador_jarvis` package, which was deleted** (same explicit decision). `pesquisar_no_navegador` opens a Google search in the default browser; `tocar_no_youtube` fetches the YouTube results page over plain HTTP (`urllib`), extracts the first `videoId` by regex, and opens `/watch?v=...&autoplay=1` in the default browser. **`pausar_musica`/`retomar_musica` are gone with it** — those only worked because the old package drove its own persistent Playwright page, and there is no page under our control any more; the prompt's `## NAVEGADOR E YOUTUBE` section tells the model this explicitly ("não existe pausar nem retomar, então nunca prometa isso"). The `playwright` dependency was removed from `requirements.txt` in the same pass. Still never executes JavaScript and never navigates to a URL built from unvalidated text: the Google query goes through `quote_plus`, and the video URL is only ever built from a regex-validated 11-character ID.
     - `jarvis/pacotes/pesquisa_web/` — `pesquisar_informacao_atual`, an invisible DuckDuckGo search (`ddgs`) that opens no window and never steals focus. The point of the module is the **local filter that runs before any network access**: `avaliar_necessidade_pesquisa()` checks currency markers, dynamic subjects, and changing job titles, and then a list of stable-question patterns that *block* the search. `despachar()` calls `pesquisar_informacao_atual()`, which runs that filter itself and returns `resposta_sem_pesquisa()` when the question doesn't need current data — **don't duplicate that decision in a client**. 60s in-memory cache, thread-safe. **Verified against the real service**: a currency-quote question returned real formatted results, and the identical follow-up question came back from the cache instead of hitting the network again. The filter itself was verified both ways on seven phrasings — four that must search (cotação, current officeholder, match score, forecast) and three that must not (o que é Python, quem foi Einstein, como funciona um motor).
     - `jarvis/pacotes/controle_mouse/` — `rolar_pagina`, `clicar_mouse`, `duplo_clique_mouse`, `clique_direito_mouse`, via `user32.dll` through `ctypes` (no `pyautogui`). **`mover_e_clicar()` is deliberately NOT exposed as a tool** — a voice-dictated "click at x,y" would be a blind click with none of the two protections the visual locator provides; it exists only for `clique_visual` to call after a target has been approved.
     - `jarvis/pacotes/escrita_texto/` — `escrever_no_campo_ativo`, which puts the text on the **real** Windows clipboard (`GlobalAlloc`/`SetClipboardData`, `CF_UNICODETEXT`) and then simulates Ctrl+V. That preserves accents and long text far more reliably than key-by-key simulation, at the cost of replacing whatever the user had on their clipboard — the course's original behavior, kept. 10,000-character safety limit.
     - `jarvis/pacotes/clique_visual/` — `clicar_elemento_visual`. **Was a native tool in the course client; became a package here.** Two things make it different from every other package: (1) it is the **only package that imports from another package** (`controle_mouse.acoes.mover_e_clicar`) — deliberate, one-directional, because both halves came from the same course mouse module and a second copy of the cursor-moving `ctypes` code would drift out of sync; (2) its `despachar()` **captures the screen internally**, which is why it's listed in `TOOLS_QUE_CAPTURAM_SOZINHAS` and the client holds `_mutex_funcao_visual()` around its whole dispatch (see below). The locator itself lives in `jarvis/servicos/visao/localizador_clique.py` (shared vision infra, next to the other capture functions), not inside the package — it blocks a sensitive/destructive target by keyword (`TERMOS_BLOQUEADOS`: excluir, apagar, formatar, comprar, pagar, transferir, instalar, "executar como administrador"...) **before even capturing the screen**, and refuses any result below `CONFIANCA_MINIMA` (0.78).
     - `jarvis/pacotes/agenda/` — `criar_evento_agenda`, `listar_agenda`, `cancelar_evento_agenda`. This package, not `memoria_obsidian`, is the entire calendar. Atomic writes (`.tmp` + `fsync` + `Path.replace`) and a `threading.Lock`, both from the original. **One change from the course**: the file moved from `memory/agenda.json` to `dados/agenda.json`, via `jarvis/caminhos.py` — no module in this project writes state next to its own code or counts `.parent` hops. Refuses past dates and exact duplicates, caps at `MAXIMO_EVENTOS` (20), auto-recovers from a missing/corrupt/malformed JSON file, and asks for disambiguation instead of guessing which event to cancel. **These functions create no alarms** — the prompt says so explicitly, so the model never promises one.
     - `jarvis/pacotes/consulta_acoes/` — `consultar_cotacao_acao` and `consultar_historico_acao` (Twelve Data). In the course this existed only for the OpenAI Realtime provider; here it's an ordinary package, so it works in **both** voice brains, per the user's explicit request ("adicione o Twelve Data para os 2"). Two changes from the course: `TWELVE_DATA_API_KEY` moved from `core/config.py` to the package's own `config.py` (per-package `load_dotenv()` convention), and the dict/list → string formatting lives in `__init__.py`, so `acoes.py` stays byte-identical to the original (it was written to be JSON-serialized straight into an OpenAI tool output; this project's contract requires `despachar()` to return a string). `acoes.py` deliberately never interpolates the `requests` exception or the request URL into an error message — both contain the full query string, and therefore the API key in plain text. **Verified against the real API**: a batch quote for `["AAPL", "MSFT"]` came back with both tickers in one call, a 10-candle daily history returned with the period trend computed correctly, and an invalid ticker produced the Twelve Data 404 message as text rather than an exception.

   - **Second voice brain: OpenAI Realtime (`jarvis/openai_realtime/`)** — a complete alternative to `GeminiLiveWorker`, picked by `PROVEDOR_IA` in `.env` (`gemini`, the default, or `openai`). Ported from `JARVIS COMPLETO`'s `openai_provider/live_client.py` at the user's explicit request ("o gemini está dando muito problema, por isso criei o da openai, para quando eu quiser trocar entre uma e outra"). **This is a deliberate, user-requested exception to the standing "OpenAI is only ever reachable through `delegacao_ia`" constraint** — see that constraint's updated wording below.
     - **The UI's only contact point is `_classe_do_worker()`** in `jarvis/ui/janela_principal.py`, which returns the right class; the `openai` import happens inside that function so a Gemini user doesn't pay for importing it. Everything downstream is provider-agnostic because both workers expose the same signals, the same `(session_handle=None, transcricao_inicial=None)` constructor, the same `parar()`/`solicitar_analise_tela()`/`solicitar_analise_camera()`/`enviar_texto_da_ui()`/`enviar_imagem_da_ui()`, and the same `transcricao_conversa` attribute.
     - **Tools are inherited, never rewritten** — `jarvis/openai_realtime/esquema.py` converts each package's existing Gemini `FunctionDeclaration`s to the Realtime API's flat `{"type", "name", "description", "parameters"}` shape. Note the target format: the Realtime API wants that flat shape, **not** the chat-completions one that nests everything under `"function"`. A new package therefore works in **both** voice brains as soon as it enters `PACOTES_REGISTRADOS`.
     - **Uses the same system prompt as the Gemini worker**, including the keyword auth block when `EXIGIR_AUTENTICACAO` is on. The course's OpenAI prompt had no auth block at all; keeping two different prompts would have been a second way to bypass the gate, which the constraint below forbids. Verified in both directions (flag on → the exact `prompts.bloco_autenticacao()` text is present; flag off → it isn't).
     - **What it does NOT have, and the UI knows it**: the Realtime API has no equivalent to `session_resumption`/`GoAway`, so `solicitou_reconexao`/`session_handle_atualizado` exist purely for interface parity and are never emitted — there is no automatic reconnection in this mode. Audio is PCM16 **24 kHz on input and output** (the Gemini worker's input is 16 kHz).
     - Uses the recurso `realtime` (GA), **not** `beta.realtime`: accounts migrated to the definitive API reject the old shape with `beta_api_shape_disabled`. That detail came from the course file's own comment and was kept.
     - **Not verified against a live OpenAI session.** Everything structural was tested offline (worker constructs, tool conversion produces 48 valid Realtime-shaped tools with no duplicates, prompt assembly in both auth branches, `enviar_texto_da_ui`/`enviar_imagem_da_ui` return `False` instead of raising with no session). The actual WebSocket handshake, the `session.update` payload, and the transcript event names were not exercised — no live call was made. The transcript handling in `_acumular_transcricao` uses defensive `getattr` precisely because those event names could not be confirmed live: if they ever change, the worst case is an empty `transcricao_conversa`, never an exception mid-session.

   - **`PACOTES_REGISTRADOS` moved out of `cliente_live.py`** into `jarvis/nucleo/registro_pacotes.py`. Two reasons: a second voice brain now needs the same list (and neither client can import the other just to get it), and with the list out of there, registering a new package no longer touches **any** of the three course files — it's one line in that module. The same file also exports two lists both clients consult:
     - `TOOLS_QUE_CAPTURAM_SOZINHAS` — tools whose `despachar()` captures screen/camera internally, with no image parameter (today only `clicar_elemento_visual`). The client holds `_mutex_funcao_visual()` around the **entire dispatch** of these, via a `contextlib.AsyncExitStack` (so the mutex is only acquired for those tools and the block costs nothing for every other one). This is deliberately different from `identificar_planta`/`consultar_segunda_opiniao_visual`, where the *client* captures and injects `imagem_bytes` into `args` — those keep their own separate handling. Verified: the mutex is genuinely held during dispatch, released after, released even when the package raises, and a busy mutex skips the dispatch loop entirely rather than dispatching without protection.
     - `TOOLS_SILENCIOSAS` — tools where a spoken reply is noise rather than help (`rolar_pagina`, `escrever_no_campo_ativo`, `clicar_elemento_visual`): the user asked for an ACTION on their screen, and hearing "pronto, rolei a página" after every scroll is clutter. The client sets `silenciar_audio_ate_fim_turno` **before** dispatching (the model can start generating audio while the function is still running) and discards the turn's audio; the flag resets at that turn's `turn_complete`. **The discard is folded into the same `if not self.suprimir_audio_pos_reserva and not self.silenciar_audio_ate_fim_turno:` condition**, never a separate check — a half-discard (setting `alfred_falando` without ever calling `fila_saida.put`) would leave the microphone blocked until `vigiar_travamento` force-releases it 60s later. This is the same trap already documented for `suprimir_audio_pos_reserva`.

   - **Session resumption and automatic reconnection (Gemini only)** — three features brought from `JARVIS COMPLETO`, spanning `GeminiLiveWorker` and `MainWindow`:
     - `session_resumption=types.SessionResumptionConfig(handle=self.session_handle)` on the `LiveConnectConfig`. The server hands out a resumption handle now and then (`resposta.session_resumption_update`, checked with `getattr` since it isn't on every response); the worker stores the latest and emits `session_handle_atualizado`. With `handle=None` — the normal case, a call the user started — behavior is identical to before: a fresh, clean session. The field only enables the feature; it forces no resumption.
     - `context_window_compression` with a sliding window, so a long conversation (all the more so now that it survives reconnections) isn't dropped when it hits the model's context limit.
     - **`go_away` handling.** The server warns before closing the WebSocket for lifetime reasons. `receber_audio` sets `renovacao_em_andamento`, flushes the mic queue, emits `solicitou_reconexao`, and sets `self.ativo = False` so the session closes cleanly. **It deliberately never sets `encerrou_por_falha`** — that flag means "this call died, and not because the user asked", which a routine WebSocket renewal is not; it also used to hand the conversation to the reserve brain, back when that existed. `vigiar_travamento` doesn't false-alarm on the resulting early return either: it already had an `if not self.ativo: break` right after its sleep.
     - **On the window side**: `session_handle` and `transcricao_preservada` survive only an automatic reconnection. Any manual close, voice-triggered close, error after a manual close, or window close clears both, so the user's next call starts clean. `chamada_finalizada` carries the transcript from the dying worker to the next one via the new `transcricao_inicial` constructor parameter (copied with `list(...)`, never shared by reference), and the end-of-call `salvar_resumo_conversa` is **skipped while `renovacao_em_andamento` is set** — the conversation hasn't ended, it continues in the next worker, and summarizing here would drop a memory note mid-conversation and another one on every renewal.

   - **`VisualizadorAlfred` (`jarvis/ui/visualizador_alfred.py`)** — the animated sphere from `JARVIS COMPLETO` (`ui/alfred_visualizer.py`, class renamed to match this project's Portuguese convention). A self-contained `QWidget`: all the drawing (`QPainter`, background/sphere cached into `QPixmap`, adaptive FPS 6/15/20) lives inside it. The window uses exactly three methods: `definir_status(texto)` (called from the window's own `definir_status`, alongside the side label), `definir_ativo(bool)` (in `iniciar_chamada`/`chamada_finalizada`), and `definir_nivel_audio(float)` — connected to the worker's `nivel_audio` signal, which already existed and **had no listener at all before this**.
     - Layout: the window became three columns — controls + activity log (330px fixed), the sphere (all remaining space), and the diagnostic console (340px fixed). Minimum size went 900→1180 wide, default 1040×560→1380×760. Every panel that was already there (activity log, console, audio device selects) is still there — the user chose "esfera no centro, painéis atuais ao lado" over adopting the course's whole window. Verified by rendering the real window and screenshotting it.

   - **How this adaptation was tested (271 automated checks, all passing, plus a real app launch).** Five suites, all offline except where noted: (1) *structure* — every source file compiles, all 22 packages honour the contract, every `despachar()` returns `None` for an unknown name, no tool name is duplicated across packages (45 package tools + 3 native in the OpenAI worker), both workers expose an identical public API, the settings screen's schemas are all valid; (2) *action behavior*, with real side effects in a throwaway folder inside the real Desktop — create/copy/cut/paste/rename really happened and were checked on disk, overwrite and out-of-Desktop paths were really refused, ambiguity really asked instead of guessing; the agenda ran against a temp file and the user's real `dados/agenda.json` was byte-compared before and after to prove it wasn't touched; mouse/text/browser/app were exercised only through their validation and refusal paths, so nothing clicked, scrolled, typed, or opened; (3) *client integration* — the silent-turn flag turns on for the right tools and off at `turn_complete`, the visual mutex is genuinely held during a `clicar_elemento_visual` dispatch, released after, released even when the package raises, and a busy mutex skips the dispatch entirely; `go_away` emits reconnection without marking failure; the assembled prompt contains every new section and none of the removed ones, and passes the jammed-word scan; (4) *UI and provider* — the real `MainWindow` was built, screenshotted, and driven through the whole reconnection state machine with a stand-in worker, and the OpenAI worker was constructed and its tool conversion and prompt assembly checked in both `EXIGIR_AUTENTICACAO` branches; (5) *live APIs* — Twelve Data and DuckDuckGo, described above. The app itself was launched twice for real (60s and 45s) and produced zero output — no warning, no traceback.
     - **What was NOT verified**: no live OpenAI Realtime session was opened (see that section's own note), no live Gemini call was made, and the actions with irreversible side effects — clicking, scrolling, typing into the active field, opening an app or a browser — were deliberately not fired, only their guard rails tested.

## Key constraints to preserve when editing

- The voice keyword authentication gate in the system prompt is a deliberate security/access-control feature of the assistant persona — don't strip it out during refactors. `EXIGIR_AUTENTICACAO=false` in `.env` is the one sanctioned, explicit, default-safe way to disable it (see `jarvis/nucleo/config.py` / the `bloco_autenticacao` splice in `executar()`) — don't add a second way to bypass it, and don't change the default away from `true`.
- `instrucao_sistema`'s body and `bloco_autenticacao` now live in `jarvis/nucleo/prompts/gemini_live_sistema.md` and `gemini_live_autenticacao.md` (see the "Prompts" section above), not as Python string-literal concatenation in `cliente_live.py` — but the trailing-space discipline still matters for a different reason there: `prompts._carregar()` skips blank lines and `##`-prefixed section headers, then joins every remaining line by appending its own separating space, so a missing space at a line's end can no longer jam two words together (the loader supplies it regardless). When editing either `.md` file, the still-useful sanity check is the same one that already caught a real regression once: re-scan the assembled text for a lowercase letter directly followed by an uppercase one (a proper noun's internal capital, like "OpenAI" or "YouTube", is a false positive — verify each hit by eye rather than assuming it's a jam).
- Audio playback must stay glitch-free: no `asyncio.sleep` was deliberately added inside the `reproduzir_audio` write loop (see comment in that method) — don't add pacing delays there. The device write must also stay on the **dedicated** single-worker executor (`laco.run_in_executor(executor_audio, ...)`), never back on `asyncio.to_thread`: the shared default pool is used by every other blocking call in the file, and a full pool delays an audio block by seconds — measured 1,69s, about 40 blocks of speech — which the user experiences as the assistant freezing mid-sentence with the CPU idle.
- Never make `capturar_camera_bytes()` (or any new camera consumer) open its own `cv2.VideoCapture(0)` while the shared handle might already be open — always check `camera_compartilhada_esta_aberta()` first and read via `ler_frame_camera_compartilhada()` when it's `True`. Confirmed live that two concurrent handles interfere with each other (see `jarvis/pacotes/camera_preview/`'s section above); a second handle opened while `JanelaCamera`'s preview is active isn't just wasteful, it visibly breaks the preview.
- **Any new code path that captures a screen or camera frame outside of `processar_funcao_visual` must go through `async with self._mutex_funcao_visual() as mutex_livre:` first** (see the "Concurrent function-call execution" section above for the bug this closed) — checking `if not mutex_livre:` and returning/reporting a "tente de novo" message instead of capturing. Never call `_mutex_funcao_visual()` from inside `processar_funcao_visual` itself, or from a function it calls (`enviar_tela_para_gemini`/`enviar_camera_para_gemini` when invoked *by* `processar_funcao_visual`) — the mutex is already held on that path, and acquiring it again would report "busy" against itself. `solicitar_analise_tela`/`solicitar_analise_camera` (the UI buttons) must keep calling `processar_funcao_visual(..., origem="botão")` rather than calling `enviar_tela_para_gemini`/`enviar_camera_para_gemini` directly — that direct-call form is exactly what reintroduced this bug for the button path.
- `admin_terminal`'s and `rede_jarvis`'s command logs (`comandos_admin.log`/`comandos_remotos.log`) must keep calling their own `_aparar_log_se_necessario()` before every append — don't remove the size cap or raise `LIMITE_TAMANHO_LOG_BYTES` as a way to "fix" a log that grew too fast; investigate why it's growing that fast instead.
- Any new `.env`-reading module (inside `jarvis/pacotes/` or not) should get a `config_schema()` and a line in `jarvis/pacotes/configuracoes/pacotes.py`'s `PACOTES_COM_CONFIG` — this list is not restricted to `jarvis/pacotes/` modules (see `jarvis/nucleo/config.py` and `jarvis/servicos/email/`'s two modules, added specifically to close that gap), so there's no excuse to skip it for a module living elsewhere.
- `navegador_web` must never execute arbitrary JavaScript or navigate to a URL constructed from unvalidated user text — an explicit user requirement carried over from the `navegador_jarvis` package it replaced. The two sanctioned URL shapes are the Google search built with `quote_plus`, and `youtube.com/watch?v=<id>` where `<id>` came from the `"videoId":"([a-zA-Z0-9_-]{11})"` regex — never a URL taken verbatim from spoken text.
- If `tocar_no_youtube` starts opening the results page instead of a video, the regex in `navegador_web/acoes.py::_extrair_video_id` stopped matching YouTube's current HTML — re-verify it against a real fetched results page before changing it, don't guess a new pattern from memory. The fallback (open the normal results page) is intentional and must stay: it always resolves to something usable.
- The three `self.alfred_falando` checks in `enviar_microfone` (the `sounddevice` callback, `adicionar_audio()`, and the send loop before `send_realtime_input`) must stay in the form `if self.alfred_falando and not self.interrupcao_habilitada:` — never revert any of the three to a bare `if self.alfred_falando:` (that silently disables interruption everywhere except that one spot) and never add a new mic-guard elsewhere without applying the same `and not self.interrupcao_habilitada` exception, or `config.json`'s `interrupcao: true` stops actually doing anything. They used to also test `self.reserva_falando`; that half went away with the `cerebro_reserva` package. `interrupcao_habilitada` is read once in `__init__` (`jarvis/nucleo/preferencias.py`'s `interrupcao_ativa()`) and never re-read mid-call — see docs/INTEGRATION.md, "Interrupção de fala (config.json)".
- **`_conectar_sessao_gemini`'s `pilha.enter_async_context(client.aio.live.connect(...))` call must stay wrapped in `asyncio.wait_for(..., timeout=TIMEOUT_CONEXAO_GEMINI_SEGUNDOS)`** — a real, user-reported bug: "se eu iniciar a chamada e o Gemini já não funcionar, o jarvis não funciona." A connection attempt that never raises AND never succeeds (the server not responding to the handshake at all, as opposed to a prompt connection error, which the fallback-model logic already handled) hung forever with no fallback and no fallback to the reserve — this happens *before* any call task exists yet, so none of `vigiar_resposta_lenta`/`monitorar_conexao`/`vigiar_travamento` are running yet to catch it; the call just sat on "Conectando ao Gemini Live..." indefinitely. `str(erro) or type(erro).__name__` (not `erro or type(erro).__name__` — the exception *object* is truthy even when its `str()` is empty, same pitfall already fixed once for `_tarefa_supervisionada`) keeps the printed message from ending in an empty string for a bare `TimeoutError`.
- **`_conectar_sessao_gemini` must only retry with `GEMINI_LIVE_MODEL_FALLBACK` when the CONNECTION ITSELF fails** (an exception from `pilha.enter_async_context(client.aio.live.connect(...))`, i.e. from `__aenter__`) — never when something fails *after* a session is already established. This is why it uses a `contextlib.AsyncExitStack` passed in by the caller instead of a plain `async with client.aio.live.connect(...) as sessao:` — the latter can't distinguish "never connected" from "connected, then died mid-call" (an exception raised inside the body would be thrown back into the same `except` an `@asynccontextmanager`-based version would use, incorrectly triggering a second, confusing "reconnect with a different model mid-conversation" instead of the existing, already-tested mid-call failure handling). A mid-call failure must keep working exactly as before (`self.encerrou_por_falha` set by the existing call sites, reported at the end of `executar()`) — don't route it through this method.
- `receber_audio`'s `if resposta.tool_call:` handling must never go back to a direct `await self.processar_chamada_de_funcao(...)` — that reintroduces the exact "one slow function freezes the whole session" bug this was fixed for. Always create it as its own `asyncio.Task` (via `_executar_chamada_de_funcao_com_timeout`), tracked in `self.tarefas_funcao_ativas`, and let `_ao_finalizar_tarefa_funcao` (its `add_done_callback`) remove it when done.
- Any function added to `TIMEOUTS_TAREFA_FUNCAO_POR_NOME` must use a value that safely exceeds that function's own worst-case internal wait (with margin) — never the flat `TIMEOUT_TAREFA_FUNCAO_SEGUNDOS` default for something that's already known to legitimately run longer, and never a value at or below that function's own internal timeout. Getting this wrong silently regresses whatever made that function need extra time in the first place (this is exactly why `executar_comando_admin`/`confirmar_comando_admin` are in that map — see the "Concurrent function-call execution" bullet above).
- `processar_chamada_de_funcao` must never go back to calling `sessao.send_tool_response`/scheduling `encerrar_apos_resposta` itself — it must stay a pure `return (function_responses, encerrar_depois)`, with `_executar_chamada_de_funcao_com_timeout` as the only place that decides how to deliver them (normal `tool_response` in Phase 1, spontaneous announcement in Phase 2 — see the "two-phase hybrid response" bullet above). And never call `sessao.send_tool_response` a second time for a `chamada.id` that Phase 2 already answered with a provisional response — there's no tested way to answer an already-answered function call on the Live API, which is exactly why Phase 2's real result goes out via `_enviar_anuncio_espontaneo` instead.
- Every call to `sessao.send_client_content(...)`/`sessao.send_realtime_input(...)`/`sessao.send_tool_response(...)` (or `self.sessao....`) must go through `await self._enviar_para_sessao(sessao.send_client_content(...))` — never call any of them directly with a bare `await`. `send_tool_response` was the one that got missed originally, in `_enviar_resposta_funcao` and `_responder_falha_para_lote`, and it froze calls permanently (the model cannot speak again until it receives that response — see the "Silently-dying call tasks" bullet above). A new direct call site reintroduces the exact "connection stalls silently, app hangs forever, needs Task Manager to kill" bug `_enviar_para_sessao`/`monitorar_conexao` exist to fix. And `monitorar_conexao` itself must never try to announce anything by voice before setting `self.ativo = False` — a spontaneous announcement is itself a `send_client_content` call, so attempting one there could hang on the exact same stalled-connection problem being recovered from.
- `reproduzir_audio`'s device write must stay wrapped in `asyncio.wait_for(asyncio.to_thread(saida.write, ...), timeout=TIMEOUT_ESCRITA_AUDIO_SEGUNDOS)`, and `receber_audio`'s outer `while` must keep the `recebeu_algo` guard. Both protect against stalls that raise **nothing** (a sound device that stops consuming; a `receive()` that returns without yielding) — the supervisor cannot catch those, and each one leaves the call looking alive while permanently mute or busy-spinning.
- The `self.timestamp_ultimo_bloco_microfone = time.monotonic()` line must stay the **first** statement of `enviar_microfone`'s `sounddevice` callback, above every early return. It measures whether the device is still delivering blocks, not whether the audio is being forwarded — moved below the `alfred_falando` return, a long spoken answer would be indistinguishable from a dead microphone and `vigiar_travamento` would end healthy calls.
- `vigiar_travamento` must only ever check states that are impossible in normal operation, never "nothing has happened recently" — an idle call with nobody talking is the normal case, and turning that into an alarm would make the watchdog noise instead of signal. Any new check added there needs the same property, plus a once-per-episode guard if the condition persists across ticks.
- Every one of the five concurrent tasks `executar()` creates must stay wrapped in `self._tarefa_supervisionada("NOME", ...)`. `executar()` does not await them until the call is already ending (`asyncio.gather(..., return_exceptions=True)` after `self.ativo` is `False`), so an unwrapped task that raises dies unobserved and leaves the call looking connected while it is permanently frozen — the confirmed root cause of a real "status parado, tive que reiniciar" report. Never catch `asyncio.CancelledError` inside that supervisor: it is not an `Exception` subclass, and the normal shutdown cancellation must keep passing through untouched. `receber_audio` in particular must never go back to having no error handling at all.
- In `_executar_chamada_de_funcao_com_timeout`, `_enviar_resposta_funcao` must stay in the FASE-1 `try`'s **`else:`** clause, never back inside the `try` body. Inside the `try`, a `TimeoutError` from the *send* is indistinguishable from "the function is still running", so it would emit the provisional "em andamento" `FunctionResponse` and then also run FASE 2 — answering the same `chamada.id` twice, which the Live API does not support.
- `enviar_microfone`'s `with sd.RawInputStream(...):` (opening the mic, plus its whole listen loop) must stay wrapped in `try`/`except Exception` — reporting via `self.erro_recebido` and setting `self.ativo = False` on failure. Removing that wrapper brings back a real, confirmed bug: the mic-open failure (or any other exception in that loop) kills the task silently, and the call is left looking connected while permanently unable to hear the user, with no error shown anywhere.
- The wake-word detector (`jarvis/pacotes/ativacao_voz/`) and `GeminiLiveWorker.enviar_microfone()` must never hold the microphone at the same time — `ativacao_voz.pausar()` (blocking, waits for the detector's stream to actually close) must stay the first statement of `executar()`, and `ativacao_voz.retomar()` must stay after `self.sessao = None` at the end. Don't reorder either past where the mic stream actually opens/closes, and don't add a new call path that opens the mic without going through this handoff.
- Voice activation (`jarvis/pacotes/ativacao_voz/`) is an *additional* way to start a call — never remove or disable the manual "INICIAR CHAMADA" button/`alternar_chamada()` path in favor of it. The feature must also degrade gracefully (print a warning, keep the button working) when the Vosk model fails to load or the mic can't be opened, not raise or block startup — and model loading must stay inside the detection thread (`_loop_deteccao`), never moved into `iniciar()`/`_abrir()`, so a slow first-time download can never block `main.py`'s startup.
- Vision and email functions are intentionally *not* auto-triggered by the model — the system prompt explicitly restricts them to explicit user requests, and for `preparar_email` specifically requires the recipient/subject/body to have been stated by the user rather than invented. Preserve these restrictions when adjusting `instrucao_sistema`.
- **`salvar_memoria` is the one deliberate exception to the "no auto-trigger" rule above — changed by explicit user request.** The system prompt (`## MEMÓRIA` in `gemini_live_sistema.md`) now tells the model it may call `salvar_memoria` on its own, mid-conversation, when a durable, genuinely useful fact about the user comes up naturally — with guidance to skip passing/trivial remarks ("na dúvida, não salve"). `esquecer_memoria` stays explicit-request-only, unchanged — this exception is only about saving, never forgetting. Don't extend this same autonomy to vision or email without an equally explicit ask; those two remain request-only for the reasons already documented (`preparar_email`'s two-step confirmation exists precisely because prompt-only discipline wasn't reliable enough on its own).
- **Sending an email is always a two-step, code-enforced confirmation — never re-collapse this into a single tool call.** `preparar_email` must never send anything itself, and `confirmar_envio_email` must never be reachable without a live `self.email_pendente` set by a prior `preparar_email` call in the same worker instance. This exists specifically because prompt-only instructions ("ask before sending") weren't reliable enough — the model could act before finishing listening to the full request. If `enviar_email` (the underlying `jarvis/servicos/email/remetente.py` function) is ever called directly from a new code path added later, that path bypasses this gate entirely; don't add one without deciding that's intentional.
- No file in this repo ever writes real emails during tests/dev — `jarvis/servicos/email/remetente.py`'s SMTP calls are only exercised against a real server when the user actually triggers it through a live call with real `.env` credentials.
- `jarvis/pacotes/rede_jarvis/comandos.py`'s `abrir_app` and `buscar_arquivo` must stay whitelist-only (`config.WHITELIST_APPS`, `config.PASTAS_PERMITIDAS_BUSCA`) — never execute an arbitrary command/path coming from an MQTT message, even though the message passed the shared-token check. Every message without the correct `TOKEN_REDE_JARVIS` is dropped silently (no reply), by design — don't add an error response for that case, it would confirm the channel's existence to a probing attacker. This whitelist-only rule applies to any remote-command surface added later (`rede_jarvis` or otherwise), not just these two functions.
- `rede_jarvis`'s Google Drive fallback path (large-file transfer): auth against a real Service Account was verified (connects fine, `client_email` reads correctly), but the upload+share+download+cleanup path itself is still untested end-to-end. Also, a real Service Account tested here came back with `storageQuota: {limit: "0", ...}` — service accounts on non-Workspace (personal) Google Cloud projects typically have no personal Drive storage, so `files().create()` may fail with a quota error even with everything else configured correctly. A Shared Drive (Workspace) the service account has access to would likely be needed; this hasn't been set up or tested.
- Tuya `TUYA_ACCESS_ID`/`TUYA_ACCESS_SECRET` (and any other cloud/broker credential in this project) must never be hardcoded — always read from `.env` via `config.py`, per module. `jarvis/pacotes/casa_inteligente/config.py` follows this already; keep it that way if `casa_inteligente` grows a second provider (`lg_thinq.py`, `xiaomi.py`, etc. — planned as sibling files, not yet built).
- **New tools/integrations always live in their own isolated package under `jarvis/pacotes/`** (`jarvis/pacotes/rede_jarvis/`, `jarvis/pacotes/casa_inteligente/`, `jarvis/pacotes/delegacao_ia/` are the existing examples), never as business logic dropped into one of the three course-project files. Every such package exposes exactly `obter_function_declarations()` and `despachar(nome_funcao, argumentos)` — the standard contract in **docs/INTEGRATION.md** — so it can be wired into any client file with the same three touch points, not bespoke code per package.
- The three course-project files (`main.py`, `jarvis/gemini/cliente_live.py`, `jarvis/ui/janela_principal.py` — formerly the `_basic`-suffixed ones) are temporary and will be fully replaced once the finished course project lands — **edit them as little as possible**. For a package, that now means only: (1) one line in `jarvis/nucleo/registro_pacotes.py` and (2) an update to `jarvis/nucleo/prompts/gemini_live_sistema.md` describing the new tool(s) to the model — **neither of which is one of the three files**. `PACOTES_REGISTRADOS` used to live inside `cliente_live.py`; it moved out precisely so that registering a package stopped touching them at all. Anything beyond that (new business logic, new state, new helper methods) belongs inside the package itself, not in one of those three files. Packages that need session-glue callbacks (like `rede_jarvis`) are the only sanctioned exception, and even those are documented as copy-paste snippets in docs/INTEGRATION.md, not open-ended edits.
- **docs/INTEGRATION.md must be updated every time a package is added or its integration surface changes** — new/changed `obter_function_declarations()`/`despachar()` behavior, a new or changed session-glue callback, etc. It is the single source of truth for re-wiring packages into a future (post-course) client file; letting it drift out of sync defeats its purpose.
- **Qt threading discipline**: `GeminiLiveWorker` (a `QThread`) never touches UI widgets directly. All communication back to `MainWindow` goes through its `Signal`s (`status_recebido`, `erro_recebido`, `chamada_encerrada`, `solicitou_encerramento`, `nivel_audio`), connected once in `jarvis/ui/janela_principal.py`; the worker only ever calls `self.<sinal>.emit(...)`. Preserve this direction — don't add a reference from the worker (or a package it calls) back into `MainWindow`/widgets, and don't call Qt GUI classes (e.g. `QFileDialog`) from a background thread without the same signal/queued-connection bridge pattern `jarvis/pacotes/rede_jarvis/transferencia_arquivos.py` already uses (`_PonteSalvarArquivo`, instantiated on the GUI thread via `preparar_ponte_gui()`).
- **Remote commands are always whitelist-only, never freeform.** Any tool that lets one machine/agent trigger an action on another (app launch, file search, shell-adjacent capability) must resolve through an explicit allowlist defined in that package's own `config.py` (mirroring `rede_jarvis.config.WHITELIST_APPS` / `PASTAS_PERMITIDAS_BUSCA`) — never execute a name/path/command taken verbatim from an incoming message, even one that already passed a shared-secret/token check.
- **OpenAI has exactly TWO sanctioned entry points, and no third.** (1) `delegacao_ia.delegar_tarefa(tipo_tarefa="segunda_opiniao", ...)` — the single, rarely-used, no-fallback route in `jarvis/pacotes/delegacao_ia/roteador.py`, deliberate because OpenAI is the most expensive provider in use. (2) `jarvis/openai_realtime/cliente_realtime.py`, the alternative voice brain, reachable ONLY when the user sets `PROVEDOR_IA=openai` in `.env` — added at the user's explicit request after being told about this constraint, not as a drive-by. Don't add a third code path that calls OpenAI directly from anywhere else, and don't make the Realtime worker reachable by anything other than that one `.env` variable (`usar_provedor_openai()` in `jarvis/nucleo/config.py`, read by `_classe_do_worker()` in `jarvis/ui/janela_principal.py` — that is the whole surface).
- `memoria_obsidian` must never write outside `PASTA_VAULT_JARVIS`. Every write and delete goes through `notas.caminho_seguro()`, which raises rather than returning a path outside the vault — a note title is model-controlled input, so this is the only thing standing between a crafted title and the rest of the filesystem.
- **A note is never deleted before being summarized.** The order in `consolidacao.consolidar_arquivo()` — generate summary → write summary to disk → only then delete the originals that went into it — is load-bearing, not stylistic. Any failure before the write must leave every file in place (two real API failures during testing confirmed it does). Never add a path that deletes archived notes without a summary, and never delete notes that were not in the summary just generated.
- `pinned: true` is an absolute exemption from pruning, and the three prune criteria (`last_used` age **AND** `access_count` **AND** not pinned) must keep being required together — loosening any one of them starts discarding memories the user still relies on.
- The mass-delete refusal in `memoria_obsidian/esquecer.py` (`COMANDOS_APAGAR_TUDO`) is a security rule inherited from the old memory manager, not a technical limitation — don't drop it, and keep returning candidate lists instead of guessing which note to delete.
- **Avoid unnecessary disk writes.** Vision *capture* (`jarvis/servicos/visao/captura_tela.py`/`jarvis/servicos/visao/captura_camera.py`) deliberately returns in-memory JPEG bytes and never writes a file on its own; `jarvis/servicos/memoria/gerenciador.py` is the one legitimate persistent store (atomic `.tmp`-then-`replace` writes, guarded by a lock), `jarvis/pacotes/rede_jarvis/comandos.py`'s `ARQUIVO_LOG` is the one legitimate append-only log, and `screen_capture.salvar_print_bytes()` / `camera_capture.salvar_foto_bytes()` are the two legitimate cases where a capture's bytes get written to disk — but only because saving *is* the entire point of the `salvar_print_tela`/`tirar_foto_camera` tools that call them; every other tool that touches a screen/camera capture (`analisar_tela`, `analisar_camera`, continuous viewing, the vision cross-check flows) still never writes anything. Don't add new on-disk artifacts (temp files, caches, logs) as a shortcut inside a tool's request/response cycle — keep data in memory unless persistence is the actual point of the feature.
- **Portuguese-only convention is not optional**: every identifier, comment, docstring, UI string, and `instrucao_sistema`/tool-result string added to this codebase must be in Brazilian Portuguese, matching the existing files — this includes new packages, not just edits to existing ones.
- `admin_terminal`'s Scheduled Task elevation mechanism (`python -m jarvis.pacotes.admin_terminal.setup`) must **never** be invoked automatically by any code path — creating (or removing) it is a standing, persistent change to the machine's own configuration and stays a manual, explicitly-confirmed action the user runs themselves in a terminal. Don't add a code path that calls `setup.criar_tarefa()`/`schtasks /create` from inside the app.
- `admin_terminal` and `rede_jarvis` stay disconnected on purpose (see `jarvis/pacotes/admin_terminal/`'s section above) — don't add `executar_comando_admin`/`confirmar_comando_admin` to `jarvis/pacotes/rede_jarvis/comandos.py`'s `TABELA_COMANDOS`, and don't have either package import from the other, without an explicit decision from the user first (remote-triggered admin execution is a materially different risk than local voice-triggered execution).
- `painel_dispositivos` must keep storing the device **name** in `config.json` and resolving it to an index at load time — never store the index, which changes whenever any audio device is connected or removed. It must also keep applying the choice via `sd.default.device` rather than passing `device=` at each stream: that single assignment is what lets the call mic, playback and the wake-word detector all honour the selection without any of them being modified.
- The console panel must keep echoing `sys.stdout`/`sys.stderr` rather than being called from each print site, must keep crossing threads via its `Signal` (never touch the widget directly from a worker thread), and must keep its line cap — those three are what make it safe to leave on permanently. It must also keep styling itself in `painel_console.py`: a rule moved into `ESTILO_GLOBAL` dies silently, because of the `#`-comment bug documented above.
- `jarvis/ui/janela_principal.py` never gains new UI (buttons, menus, dialogs) for a package's feature — any package that needs to show its own window emits a `Signal` on `jarvis.nucleo.sinalizador` instead (see `jarvis/pacotes/configuracoes/`'s section above and docs/INTEGRATION.md), connected from `main.py`. This keeps the main window ignorant of every package built on top of it, matching the same "don't touch the course-project files beyond the standard touch points" principle applied to the UI layer specifically.
- Never print, log, or otherwise surface a sensitive `.env` value (API key, token, password, secret) in plain text outside the masked field it belongs to — this applies to `jarvis/pacotes/configuracoes/window.py` specifically (a sensitive field's value must never appear in a `print()`, an exception message, or anywhere but that one `QLineEdit`), on top of the project-wide rule of never hardcoding or echoing credentials.
- `jarvis/pacotes/rede_jarvis/visualizacao_remota.py`'s `MonitorTelaContinuo` instantiation must keep **not** passing `funcao_captura` (i.e. stay on the fixed-primary-monitor default) — don't make remote screen viewing follow the cursor without an explicit decision from the user; see `jarvis/servicos/visao/monitor_continuo.py`'s section above for why the two consumers of this class are intentionally allowed to diverge.
- Don't "fix" the existing `send_client_content` mid-session calls (`enviar_camera_para_gemini`, `enviar_tela_para_gemini`, `_enviar_anuncio_espontaneo`, `enviar_imagem_para_cruzamento`) to use `send_realtime_input` as a drive-by change while touching this file for something else, even though `send_client_content` is technically outside the documented contract for the currently-configured model (see `jarvis/pacotes/chat_jarvis/`'s section above) — they're tested working, and migrating them is a deliberate, separate decision the user hasn't made yet, not an opportunistic cleanup.
- `fechar_app`'s `PROCESSOS_PROTEGIDOS` list (`explorer.exe`, `winlogon.exe`, `csrss.exe`, `services.exe`, `svchost.exe`) plus the by-PID self-protection of the ALFRED process itself must never be weakened or bypassed — no new code path in `fechador.py` may close a process without first checking against this list (by exact name, never prefix/substring) and the running PID. Keep the self-protection check on `os.getpid()`, not on a process-name match against `python.exe`/`pythonw.exe` — a name-based check would also block unrelated Python processes that have nothing to do with ALFRED.
- `fechar_app` must keep trying `WM_CLOSE` (graceful) before `terminate()`/`kill()` (forced) — never skip straight to forced termination as a shortcut. The graceful attempt is what lets an app prompt "save before closing?" when it has unsaved work; only a genuinely unresponsive process (or one with no window at all) should ever reach the forced path.
- **`arquivos_area_trabalho` must never gain a function that deletes or overwrites.** Every path in `acoes.py` goes through `_esta_dentro_da_area`/`_resolver_caminho_relativo` (which reject anything outside the Desktop), every create uses `exist_ok=False`, and every paste/rename checks `destino.exists()` first. There is no `unlink`/`rmtree`/`os.remove` anywhere in that file and there must not be one — this is the module's whole security model, inherited from the course project and reinforced in the prompt's `## SEGURANÇA DAS AÇÕES LOCAIS` section.
- **`controle_mouse.acoes.mover_e_clicar` must never become a voice-callable tool.** It is only ever called by `clique_visual`, and only after `localizador_clique` has approved the target — a spoken "click at x,y" would be a blind click with neither the blocked-terms list nor the confidence threshold protecting it. The same goes for the locator's own guards: `TERMOS_BLOQUEADOS` is checked *before* the screen is even captured, and `CONFIANCA_MINIMA` (0.78) must not be lowered to make the tool "work more often".
- Any new tool whose `despachar()` captures the screen or camera **by itself** must be added to `TOOLS_QUE_CAPTURAM_SOZINHAS` in `jarvis/nucleo/registro_pacotes.py` — that list is what makes the client hold `_mutex_funcao_visual()` around its dispatch. A tool that instead receives the image as a parameter belongs to the `identificar_planta`/`consultar_segunda_opiniao_visual` pattern, where the *client* captures under the mutex and injects `imagem_bytes` into `args`; don't mix the two.
- `silenciar_audio_ate_fim_turno` must keep discarding a turn WHOLE — the single `if not self.silenciar_audio_ate_fim_turno:` in `receber_audio` guards `alfred_falando`, `fila_saida.put` and the mic clearing together, and they must not be split. Setting `alfred_falando` without ever calling `fila_saida.put` leaves the microphone blocked until `vigiar_travamento` force-releases it up to `LIMITE_FALANDO_PRESO_SEGUNDOS` (60s) later. It must also keep resetting at `turn_complete`. (This condition used to also carry `suprimir_audio_pos_reserva`, which went away with the `cerebro_reserva` package.)
- **`go_away` must never set `self.encerrou_por_falha`.** It is a routine WebSocket renewal, not a failure; marking it as one would report a failure to the user on every routine rotation of the connection. For the same reason, the end-of-call `salvar_resumo_conversa` stays skipped while `renovacao_em_andamento` is set — the conversation continues in the next worker, and the transcript is carried over through `transcricao_inicial` instead.
- `session_handle` and `transcricao_preservada` on `MainWindow` must keep being cleared on every manual close, voice close, and window close — they survive ONLY an automatic reconnection. A call the user starts must always begin a fresh conversation; leaking a stale handle into it would silently resume an old session the user thought they had ended.
- The `OpenAIRealtimeWorker` must keep exposing exactly the same public API as `GeminiLiveWorker` (the seven signals, the `(session_handle, transcricao_inicial)` constructor, `parar`, `solicitar_analise_tela`, `solicitar_analise_camera`, `enviar_texto_da_ui`, `enviar_imagem_da_ui`, `transcricao_conversa`). That parity is the only reason `jarvis/ui/janela_principal.py` can switch providers with one function; adding a provider-specific method that the UI calls would break it. It must also keep building its tools by CONVERTING `PACOTES_REGISTRADOS` and its prompt from `jarvis/nucleo/prompts/` — never a second hand-written copy of either, and never a prompt without the auth block when `EXIGIR_AUTENTICACAO` is on.
- `criar_arquivo` must never write outside `config.pastas_permitidas()` — `pasta` is always resolved against that allowlist's folder *names*, never treated as a raw filesystem path from voice/model text. Keep both containment checks in `escritor.py` (the sanitized single-component filename, and the separate resolved-path-inside-destination-folder check right before the write) — neither replaces the other.
- `explorador_windows.obter_arquivo_selecionado()`'s Explorer-window check and its Desktop fallback must stay in that order, with the `None`-vs-`(False, mensagem)` distinction preserved: only "no Explorer window matched the foreground window at all" (`None`) falls through to the Desktop; "an Explorer window matched but had nothing selected" (`(False, mensagem)`) must keep returning that message as-is, since the user's foreground window was unambiguous in that case.
- `abrir_aplicativo`'s `PASTAS_EXTRAS_APPS` scan (`acoes.abrir_de_pastas_extras()`) must stay a shallow scan (the folder itself plus one level of subfolders) — don't switch it to an unbounded recursive walk (`rglob`), which could make a simple voice command take an unpredictably long time on a large folder tree. It must also stay the LAST of the five resolution steps: the four steps before it are what Windows itself already knows about, and they should always win.
