# Infraestrutura compartilhada: código reutilizável que não é uma
# integração/ferramenta por si só, e que tanto o núcleo quanto os
# pacotes de jarvis/pacotes/ consomem.
#
#   visao/    -> captura de tela, câmera e streaming contínuo
#   email/    -> envio (SMTP) e leitura (IMAP)
#   memoria/  -> memória conversacional persistente
